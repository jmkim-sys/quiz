# SPEC (Functional Specification) — 기능 명세서
## STORY 퀴즈 프롬프트·생성·검수 과정 자동화

**작성일**: 2026-07-30 (v1.1 개정: 2026-07-30, 128건 규모 확장 — 목차 자동 매칭, 대단원별 누적 구조)
**문서 버전**: v1.1
**관련 문서**: [PRD_요구사항정의서.md](./PRD_요구사항정의서.md), [PLAN_실행계획서.md](./PLAN_실행계획서.md), [CLAUDE.md](./CLAUDE.md)

---

### 1. 시스템 구성 개요

```
[재료]                        [처리]                              [산출물]
STORY 원고(.docx)      →   목차 자동 매칭(퀴즈생성자)      →   대단원별 누적 CSV
+ 프롬프트 템플릿(.md)      + 프롬프트 템플릿 적용                (산출물/대단원별/*.csv,
+ 목차 CSV(분과/대단원/       + 자체 검수 17항목 + 지향 3항목        해당 아티클 7행만 채움)
  중단원/아티클/로그라인)     + 사람 확인("확인했어")
(수정 금지, 읽기 전용)        + 대단원누적자가 실제 반영
```

128건 규모로 확장하면서(v1.1), 원고별로 개별 산출물 파일을 만들던 방식을 버리고 **대단원별 누적
CSV 하나에 계속 채워나가는 방식**으로 바꿨다 — 자세한 배경은 4장 "대단원별 누적 CSV 구조" 참고.

| 산출물 | 역할 | 수정 여부 | 갱신 시점 |
|---|---|---|---|
| 재료/원고, 재료/프롬프트 템플릿, 재료/목차 CSV | 원본 입력 자료 | 절대 수정하지 않음 | 사용자가 재료 폴더에 새로 넣거나 팀이 목차를 갱신할 때 |
| 산출물/대단원별/*.csv | 누적되는 최종 결과물 | 아티클 완료 시마다 해당 7행만 갱신 | 원고 1건 처리·확인 완료 시 |
| 기록/HISTORY.md, LESSONS.md | 진행 로그 | 계속 누적 | 매 단계 처리 후 |

---

### 2. 입력 명세

- **STORY 원고**: `재료/` 안의 텍스트 또는 .docx 파일. 형식 자유(워크시트C 기준 텍스트 원고).
  총 128건이 완성되는 대로 순차적으로 들어온다 (7장 참고)
- **프롬프트 템플릿**: `산출물/퀴즈생성_프롬프트_템플릿_v5.md` — 문항 유형·순서·난이도·출력 형식·
  분과별 맞춤 가이드가 이 파일에 전부 정의되어 있으므로, 이 SPEC은 그 규칙을 새로 재정의하지 않고
  그대로 따른다. **v5가 유일한 실행용 프롬프트다**(2026-08-28 확정). `v4.md`는 원본
  `재료/iKAOS_빅히스토리_퀴즈_프롬프트_v3.md` → v4의 변경 경위를 남긴 **이력 보관용**이며, 규칙의
  근거로 인용하지 않는다 — v4 본문에는 2026-08-28 공통 지침 4건이 반영되어 있지 않다
- **목차 (v1.1 신규)**: `재료/Story전체 목차_0730.csv` — 팀 기획자가 만든 확정 커리큘럼 목차.
  전체 대단원/중단원/아티클(소단원)/로그라인/분과가 정리되어 있으며, Step 2의 메타데이터 자동
  매칭 근거로 쓴다. 작업 중 팀이 갱신할 수 있음
- **메타데이터(분과/중단원/아티클)**: 원고 파일 자체에는 보통 없으므로, 위 목차 CSV와 대조해
  Step 2에서 자동 매칭한다. 매칭 실패시에만 `[확인 필요]` 처리

---

### 3. 처리 로직

**Step 1. 재료 확인**
`재료/` 폴더에서 이번에 처리할 STORY 원고와, 기준이 되는 프롬프트 템플릿(가장 최신 버전)을
확인한다. 원고나 템플릿이 없으면 진행하지 않고 사용자에게 알린다 — 없는 것을 지어내서 진행하지
않는다.

**Step 2. 메타데이터 판단 — 목차 자동 매칭 우선 (v1.1 개정)**
`재료/Story전체 목차_0730.csv`(팀 기획자가 만든 확정 커리큘럼 목차 — 전체 대단원/중단원/아티클/
로그라인/분과가 정리되어 있음. 작업 중 팀이 갱신할 수 있으며, 갱신되면 이 매칭도 다시 확인한다)와
원고 내용을 대조해 분과·중단원·아티클을 자동으로 매칭하는 것을 1차 방법으로 한다. 목차의
"로그라인(핵심 질문/한 줄 스토리)" 열과 원고 내용이 일치하는 행을 찾아 그 값을 그대로 채운다 —
원고 내용만으로 AI가 임의 추정하던 기존 방식보다 이 매칭을 우선한다.

- 명확히 일치하는 행이 있으면: 그 값을 그대로 채우고 "목차의 어느 행과 일치했는지"를 근거로 남긴다.
- 일치 후보가 여럿이거나 애매하면: 가장 근접한 후보와 근거를 제시하되 `[확인 필요]`로 표시한다.
- 목차 어디에도 대응 내용이 없으면: 추측하지 않고 `[확인 필요: 목차에 대응 항목 없음]`으로 남긴다.

**Step 3. 퀴즈 7문항 생성**
프롬프트 템플릿에 정의된 고정 배열(①인라인/하 ②OX/하 ③사지선다 (단수)/중 ④순서배열/중-상
⑤직접 입력/상 ⑥사지선다 (복수)/중 ⑦순서배열/중-상 — 유형/난이도 순)과 유형별 작성 규칙을 그대로 적용해 문항을
만든다. 모든 문항의 정답 근거는 반드시 원고 본문 안에 있어야 하며, 원고에 없는 배경지식으로
정답을 만들지 않는다.

> 이 배열은 **생성 기준**이다. 생성 단계에서는 일곱 자리를 임의로 줄이거나 바꾸지 않는다. 다만
> **시트에 최종으로 들어갈 구성은 Step 6에서 기획자가 고른 결과가 정한다** (2026-08-31 확정).

**Step 4. 자기검수 (1차 검수)**
프롬프트 템플릿에 정의된 "출제 원칙 종합 검수" 17개 항목(행/열 개수, 배열 일치 여부, 정답 근거,
3·6번과 4·7번 중복 여부, 정답 표기 형식, 예문·정답해설 처리, 정답해설 톤, 단순 연도 암기 여부,
분과별 맞춤 가이드 반영 여부, 문장의 정보량과 결, 한국어 문장 구조, **순서배열 근거, 정답·보기의
비유 금지, 직접 입력 발문·예문 형식**)을 하나씩 점검해 통과/미통과를 리포트로 남긴다.

**필수 17항목과 별개로 「☆ 모범 문항 지향 항목」 3개(18~20번)를 함께 보고한다.** 기획자 검수에서
모범 문항(별표)으로 표시된 문항들의 공통 특징을 뽑은 것이며, **지향점이지 합격 조건이 아니다.**
보고 표기는 `달성` / `해당 없음` / `미달성 — 사유`이고 `통과`/`미통과`를 쓰지 않는다.
**미달성이어도 문항을 고치지 않으며, 미달성을 이유로 결과물을 되돌리지 않는다.** 원고 조건상
그 형태를 만들 수 없다는 정보로만 취급한다. (2026-09-01 신설 — 근거는 v5 「☆ 모범 문항 지향 원리」)

**Step 5. 사람 확인 지점 고정 삽입**
결과물 맨 아래에 아래 문구를 항상 그대로 포함한다 — 삭제·수정 금지:
> ⚠️ 아래 항목은 반드시 사람이 최종 확인합니다: 과학적 사실 오류 여부 / 정답 타당성 / 최종 발행 승인

**Step 6. 사람 확인 → 대단원별 누적 반영 (v1.1 개정)**
Step 1~5는 `퀴즈생성자` 서브에이전트가 수행하며 파일로 저장하지 않는다(문항 미리보기·자기검수
리포트·사람 확인 지점을 응답으로만 반환). 사용자가 "확인했어"라고 말한 뒤에만 `대단원누적자`
서브에이전트를 호출해, 해당 아티클이 속한 `산출물/대단원별/{번호}_{대단원명}.csv`의 스켈레톤 행에
문제·정답·보기·예문·정답해설 칸을 채우고 `기록/HISTORY.md`에 처리 내역을 한 줄 남긴다. "확인했어"
전에는 다음 단계로 넘어가지 않는다.

**기획자 선택이 기준 배열과 다를 때 (2026-08-31 확정).** 스켈레톤의 7문항 배열은 **기준 구조**이고,
최종 구성은 기획자가 "CSV 반영해줘"로 보낸 선택이 정한다. 선택이 기준 배열과 맞지 않으면 — 한 자리에서
두 개를 고르거나, 어떤 자리는 하나도 고르지 않았거나 — **선택을 깎지 않고 그 아티클 블록의 행 구조를
선택에 맞춰 고쳐서 반영한다.**
- **문항 수 7개는 고정값이다** — 블록은 언제나 7행이고 문항 번호도 1~7이다. 달라지는 것은
  **난이도·퀴즈 유형 배열**뿐이다. 선택이 7개가 아니면 반영하지 않고 사람에게 알린다.
- **문항 번호는 고른 순서대로 1부터 다시 매기고**, 난이도·퀴즈 유형은 그 문항이 온 자리의 값을 따른다.
- 분과·중단원·아티클은 그 블록 첫 행에만 둔다. **다른 아티클 블록은 건드리지 않는다.**

원고 128건은 순차적으로(완성되는 대로) 들어오며 목차상의 순서와 무관하다 — 처리 순서는 항상
"사용자가 지금 처리를 요청한 원고"이며, 여러 건이 재료 폴더에 함께 있어도 한 번에 하나씩만 처리한다.

---

### 4. 데이터 구조 명세

**퀴즈 출력 TSV — 17개 열 (헤더 행 없이 7개 행만 출력)**

| # | 열 이름 | 설명 |
|---|---|---|
| 1 | 분과 | 물리/화학/생명/천문/지구환경/통합 중 하나. 1번 행에만 입력 |
| 2 | 중단원 | 커리큘럼상 중단원명. 1번 행에만 입력, 원고에 근거 없으면 [확인 필요] |
| 3 | 아티클 | 커리큘럼상 아티클명. 1번 행에만 입력, 원고에 근거 없으면 [확인 필요] |
| 4 | 문항 번호 | 1~7 고정 |
| 5 | 난이도 | 하/중/중-상/상 (문항 번호별 고정 배열) |
| 6 | 퀴즈 유형 | 인라인/OX/사지선다 (단수)/순서배열/직접 입력/사지선다 (복수) — 괄호 앞 공백까지 팀 시트·스켈레톤 CSV의 실제 표기를 그대로 따른다 |
| 7 | 문제 | 학습자에게 보이는 발문 텍스트만 |
| 8 | 정답 1 | 문항 유형별 정답 표기 규칙 준수 (아래 표 참고) |
| 9 | 정답 2 | 인라인(빈칸[2] 정답), 사지선다 (복수)에서 사용. 그 외 유형은 빈칸 |
| 10~15 | 보기 1~6 | 유형별로 사용 개수·묶는 방식이 다름 (아래 표 참고). 미사용 칸은 빈칸 |
| 16 | 예문 | 필요한 문항만 채움, 불필요하면 `-` (`X` 금지 — OX 정답과 혼동) |
| 17 | 정답해설 | '~해요/어요' 톤, 2~3문장, 오답 해설 없음. 특별히 적을 해설이 없어도 비워두거나 `-`로 표시 (`X` 금지 — OX 정답과 혼동) |

**정답 1·정답 2 표기 규칙 (팀 시트 작성 가이드 기준, 항상 이 방식을 따름)**

| 퀴즈 유형 | 정답 1 | 정답 2 |
|---|---|---|
| 인라인 | **보기1(빈칸[1])의 정답 번호**(보기1 칸 안에서 몇 번째인지) | **보기2(빈칸[2])의 정답 번호**(보기2 칸 안에서 몇 번째인지) |
| OX | `O` 또는 `X` | 사용 안 함 |
| 사지선다 (단수) | 정답 보기 번호 1개만 (문장 복사 금지) | 사용 안 함 |
| 사지선다 (복수) | 정답 보기 번호 중 하나 | 나머지 정답 보기 번호 (정답1·정답2 두 칸에 나눠 기입) |
| 순서배열 | 정답 순서대로 **보기 라벨**(`가`·`나`·`다`·`라`)을 하이픈(-)으로 연결. 예: `나-라-가-다` | 사용 안 함 |
| 직접 입력 | 보기 번호가 아니라 정답 텍스트 그대로 | 사용 안 함 |

**보기 1~6 표기 규칙 (팀 시트 작성 가이드 기준)**

| 퀴즈 유형 | 보기 칸 사용 방식 |
|---|---|
| 인라인 | 개별 칸에 하나씩 나누지 않음. **보기1 칸에 빈칸[1]의 선택지 전체를 슬래시(/)로 이어서 기입**, **보기2 칸에 빈칸[2]의 선택지 전체를 슬래시(/)로 이어서 기입**. 보기3~6은 사용 안 함 |
| OX | 보기1=`O`, 보기2=`X` 고정. 보기3~6 사용 안 함 |
| 사지선다 (단수·복수) | 보기1~4에 각 선택지를 한 칸씩 기입. 보기5~6 사용 안 함 |
| 순서배열 | 보기1~4에 배열 문장을 한 칸씩 기입(뒤섞인 상태). 보기5~6 사용 안 함. **화면에서 문장은 숫자가 아니라 `가`·`나`·`다`·`라`로 표시된다** — 보기1=가, 보기2=나, 보기3=다, 보기4=라 (2026-08-28 확정) |
| 직접 입력 | 보기1~6 모두 사용 안 함 (빈칸) |

**자기검수 리포트 구조**: **필수 17개** 체크 항목 × (통과 여부 + 근거/비고) **+ 지향 3개**(18~20번,
달성 여부 + 사유). 구성과 번호는 아래로 고정한다. **필수 항목 수는 17개 그대로이며, 지향 3항목은
합격 판정에 들어가지 않는다.**

- **1~10번**: 원본 템플릿의 출제 원칙 종합 검수(행/열 개수, 배열 일치 여부, 정답 근거, 3·6번과
  4·7번 중복 여부, 사지선다 정답 번호 표기, 복수 정답 신호, 직접 입력 정답 조건, 순서배열 하이픈
  표기, 예문·정답해설 빈칸 처리, 정답해설 톤)
  - **4번(중복 여부) 2026-08-28 확장**: A/B 2안일 때 "자리마다 A안과 B안이 **서로 다른 핵심 키워드
    또는 다른 학습 포인트**를 다루는가"를 함께 본다. 사람이 한 자리에서 A·B를 둘 다 고를 수 있기
    때문이다. 자리별로 A·B가 각각 어떤 키워드를 맡았는지 한 줄로 밝힌다.
    (기준: `산출물/퀴즈_AB_2안_생성기준.md` 대원칙 2-1. 7문항 단일안에는 해당하지 않는다.)
  - **7번(직접 입력 정답 조건) 2026-08-31 재개정**: 빈칸 표기가 **밑줄 `_`**로 바뀌었다. 정답의
    글자 하나를 밑줄 하나로 바꾸고 **정답에 있는 띄어쓰기는 그대로 살린다**
    (`라니냐` → `___` · `금속 결합` → `__ __` · `태양 중성미자 문제` → `__ ____ __`).
    이 한 가지 표기로 "빈칸"·"글자 수"·"띄어쓰기"를 함께 나타낸다. 밑줄 개수는 자연히 공백을 뺀
    글자 수와 같다. **표기가 띄어쓰기를 보여주는 것과 채점이 공백을 무시하는 것은 별개**이며,
    채점 규칙은 그대로다(학습자가 붙여 써도 정답). 아래 08-28 항목의 `○` 표기는 폐기됐다.
  - **7번(직접 입력 정답 조건) 2026-08-28 재개정** (표기는 08-31에 다시 바뀜): 글자 수를 명시하는 자리가 [문제] 칸에서
    **[예문] 칸의 빈칸 표기**로 옮겨졌다. 같은 날 표기 자체도 바뀌어, **정답의 글자 수(공백 제외)만큼
    흰 원 `○`(U+25CB)를 반복**하는 형태가 되었다 — 이 한 가지 표기로 "빈칸"과 "글자 수"를 동시에
    나타낸다(`금속 결합` → `○○○○`). 로마자 `O`는 OX 정답·산소 `O₂`·`d 오비탈` 같은 알파벳 표기와
    섞이므로 쓰지 않는다. 옛 표기 `[ 빈칸 ](N글자)`는 폐기. 발문·예문 형식 자체는 17번이 본다.
  - **7번(직접 입력 정답 조건) 2026-08-15 개정**: 정답은 본문에 명시된 **하나의 용어**이면 되고
    **띄어쓰기가 있어도 통과**다. 채점이 공백을 무시하기 때문이다. 글자 수는 **공백을 뺀 수**로 센다
    (`쌓음 원리` → 4글자). 확인할 것은 **원고 표기를 임의로 붙여 쓰지 않았는지**뿐이다.
    (옛 조건 "띄어쓰기 없이 원고지 칸에 들어갈 수 있어야 함"은 폐기. 이 제약 때문에 `공유 결합`·
    `수소 결합`·`복사 평형`·`비활성 기체` 같은 핵심 용어가 5번 정답 후보에서 계속 밀려났고,
    `쌓음원리`·`정역학적평형`·`양자역학`처럼 원고에 없는 표기를 만들어 내는 부작용이 생겼다.)
  - **8번(순서배열) 2026-08-15 추가**: 문장은 뒤섞인 상태로 제시되므로 **각각 단독으로 읽혀야** 한다.
    문장 첫머리에 다른 문장을 가리키는 지시어(`그 결과`·`그 충돌로`·`이렇게`)나 순서를 드러내는
    접속어(`그러나`·`그래서`·`결국`·`마침내`·`뒤이어`)를 쓰지 않는다. 그런 표현은 가리킬 대상이
    화면에 없을 뿐 아니라 **정답을 흘린다** — 과학을 이해하지 않아도 접속어만 보고 자리를 찾게 된다.
    인과는 접속어가 아니라 내용으로 드러낸다.
- **11번**: "어떤 문항도 단순 연도·숫자 암기형 정답이 아닌가" (v4 추가)
- **12번**: "입력된 분과의 맞춤 출제 가이드가 4~7번 문항에 실제로 반영되었는가" (v1.1 추가 —
  분과별 가이드는 원본 템플릿에 이미 있었으나 자기검수 항목화되어 있지 않아 위반 사례가 있었음)
- **13번**: "문제·보기·예문·정답해설이 한 문장에 정보 하나씩 담겨 매끄럽게 읽히는가" — 길고 매끄럽지
  않은 문장은 나눈다. **글자 수 상한은 없다** (2026-08-07 추가: 완성된 퀴즈를 사람이 직접 풀어본 뒤
  "한 문장이 너무 길고 매끄럽지 않다"는 피드백을 받아 항목화 / **2026-08-12 개정**: "60자 안팎·80자
  초과 시 미통과"라는 수치를 삭제했다. 아래 정정 이력 참고)
- **14번**: "템플릿 `★★ 한국어 문장 구조 검토 규칙` 13번의 **9가지 검사를 모두 통과했는가**"
  (주어·주서술어 명확 / '은·는' 중첩 시 실제 대조 / '에서는' 필요성 / 관형어 두 겹 금지 / 주어·서술어
  사이 정보량 / 비교 표현의 평행성 / 지시어 선행어 명확 / 한 번 읽고 이해 / 배경·설명·질문·조작 안내
  분리) — 2026-08-10 추가: 사람이 7문항을 직접 교정하며 제시한 한국어 문장 구조 규칙 14개를 항목화.
  13번이 **문장의 정보량과 결**을 본다면 14번은 **문장 구조**를 본다. 미통과 판정의 실질적 기준은
  14번이다.
- **15번**: "순서배열(4·7번)의 정답 순서가 **원고에 표시된 연도** 또는 **이론적으로 필연적인 인과**로
  정해지는가" — **원고의 문장 순서만이 근거인 문항은 미통과**다. 연도 표시가 없어도 앞 문장이 뒤 문장의
  전제가 되어 뒤바꾸면 성립하지 않는 관계면 허용한다. 4·7번 각각에 대해 **무엇이 근거인지**(연도인지,
  어떤 인과 사슬인지)를 한 줄로 밝힌다. 근거를 만들 수 없으면 억지로 만들지 않고 그대로 보고한다.
  (2026-08-28 추가 — 사람이 미리보기를 검토하며 "원고 문장 순서만으로 만든 순서배열은 과학을 이해한
  학습자도 풀 수 없다"고 지적해 항목화.)
- **16번**: "**정답 칸과 보기 칸**에 비유적·은유적 표현이 없는가" (예: `결합 손`) — 원고가 설명을 쉽게
  하려고 쓴 비유는 원고 안에서만 통하는 말이지 학습자가 익혀야 할 과학 용어가 아니다. **순서배열 문장도
  보기 칸이므로 함께 본다.** [문제]·[예문]에서 맥락으로 인용한 비유는 해당하지 않는다. 원고에 정식 용어
  없이 비유만 있으면 그 소재로는 문항을 만들지 않는다 — 정식 용어를 원고 밖에서 끌어오면 규칙 3(지어내기
  금지) 위반이다. (2026-08-28 추가)
- **17번**: "5번의 [문제] 칸이 `빈칸에 들어갈 알맞은 말을 쓰세요.` 한 문장이고, [예문] 칸에 설명과
  **정답을 글자 단위로 밑줄 `_`로 바꾸고 띄어쓰기를 살린 빈칸**을 포함한 완결된 문장이 있으며,
  **빈칸에 들어갈 핵심 용어가 하나로 특정**되는가"
  — 빈칸에 다른 용어를 넣어도 문장이 성립하면 미통과. 빈칸이 [문제] 칸에 있으면 옛 형식이므로 미통과.
  밑줄 표기가 정답과 글자 수·띄어쓰기까지 같지 않거나 옛 표기(`○` 반복 · `[ 빈칸 ](N글자)`)가 남아
  있으면 미통과. (표기는 2026-08-31 재확정 — 7번 항목 참고)
  (2026-08-28 추가 — 직접 입력 발문을 통일해 달라는 사람 검토 의견을 항목화. 이로써 **빈칸의 위치가
  [문제] 칸에서 [예문] 칸으로 이동**했고, 옛 규칙 "용어를 묻는 한 문장만 [문제] 칸에 둔다"는 폐기됐다.)

- **18~20번 (☆ 지향 · 2026-09-01 신설)**: 위 1~17번과 **성격이 다르다.** 합격 조건이 아니라 지향점이며,
  표기는 `달성` / `해당 없음` / `미달성 — 사유`이고 `통과`/`미통과`를 쓰지 않는다. **미달성이어도 문항을
  고치지 않고 그대로 낸다.** 필수 항목과 섞지 말고 따로 묶어 보고한다.
  - **18번**: 1번 인라인의 두 빈칸이 **논리 사슬**([1] 개념·원인 → [2] 역할·결과·이름)을 이루는가?
    적어도 한 빈칸이 **[예문]의 서술만으로** 풀리는가? (v5 「지향 1」)
  - **19번**: 정답 칸·보기 칸에서 수치를 그대로 맞히게 하는 대신 **크기 비교·순서**로 물었는가?
    실제 수치는 [정답해설]로 보냈는가? 수치 자체가 핵심 학습 내용이면 `해당 없음`. (v5 「지향 2」)
  - **20번**: 사지선다(3·6번)의 보기가 **같은 범주의 실재 용어**이며, 각 오답이 어떤 오개념을
    겨냥했는지 한 줄로 밝힐 수 있는가? (v5 「지향 3」)
  - A/B 2안이면 **필수 17항목과 마찬가지로 A안 세트·B안 세트 각각** 보고한다.
  - 지향을 달성하려고 소재를 바꾸거나 원고에 없는 내용을 끌어오면 **그쪽이 규칙 위반**이다.

> **2026-08-07 정정 이력**: 그 전까지 SPEC·에이전트 정의는 "11개 = 원본 10개 + 분과 가이드"로,
> 템플릿 체크리스트는 "11개 = 원본 10개 + 연도 암기"로 서로 **다른 항목을 11번이라 부르고** 있었다.
> 실제 점검 항목이 12개인데 문서마다 10개/11개로 적혀 있어 먼저 12개로 정리했고, 같은 날 문장
> 가독성(13번)이 추가되어 13개가 되었다. 이후 2026-08-10에 한국어 문장 구조(14번)가 추가되어
> **현행 표준은 위 1~14번**이다.
>
> **2026-08-12 정정 이력 (13번 수치 삭제)**: 13번의 "60자 안팎 / 80자 초과 시 미통과"는 **사용자가
> 정한 값이 아니었다.** 2026-08-07에 받은 피드백은 "한 문장이 너무 길고 매끄럽지 않다" 한 줄이었고,
> 숫자는 그것을 검수 항목으로 옮기는 과정에서 임의로 붙인 것이다. 이후 사용자가 직접 다듬어 확정한
> 문장(`01_빅뱅_우주의_시작.csv` 6행 예문, 96자)을 이 규칙이 미통과로 판정하는 상태가 되어, 규칙이
> 사람 판단 위에 올라가 있었다. 사용자 지시로 수치를 전부 삭제하고 원래의 정성적 기준으로 되돌렸다.
> 근거: 2026-08-10에 사람이 실제로 지적한 문제 9건은 **전부 길이와 무관한 구조·개념 문제**였고,
> 그때 길이 검사는 전항목 통과였다. **1~14번의 번호는 그대로 둔다** — 번호를 다시 매기면
> 문서 간 불일치가 재발하기 때문이다.
>
> **2026-08-28 추가 이력 (15~17번 신설, 14개 → 17개)**: 사람이 퀴즈 미리보기를 검토하며 공통 지침
> 4건을 제시해 항목화했다 — 순서배열 근거(15번), 정답·보기의 비유 금지(16번), 직접 입력 발문·예문
> 형식(17번), 그리고 A·B 핵심 키워드 차별화(번호 없이 `퀴즈_AB_2안_생성기준.md` 대원칙 2-1과 자기검수
> 4번에 반영). **기존 1~14번은 번호도 내용도 건드리지 않고 뒤에 이어 붙였다** — 위 원칙("번호를 다시
> 매기지 않는다")은 지키면서 새 항목을 추가하는 방법이다. 다만 7번은 글자 수를 적는 자리가 [문제]에서
> [예문]으로 옮겨져 문구만 고쳤다(판정 기준은 그대로).
>
> **2026-09-01 추가 이력 (지향 18~20번 신설, 필수는 17개 그대로)**: 기획자 검수에서 모범 문항으로
> 별표된 8건(5개 아티클)의 공통 특징을 분석해 **지향 원리 3가지**를 도출했다 — ① 설명을 먼저 주고
> 이름을 묻기 ② 수치는 해설로 보내고 보기에서는 관계를 묻기 ③ 오답을 같은 범주의 진짜 개념으로 만들기.
> 8건 중 5건이 1번 인라인이었고 5개 아티클 전부에서 1번이 별표를 받았다. **사용자 지시로 이것을
> 필수 규칙이 아니라 지향점으로 넣었다** — 원문 그대로 인용하면 "이런 문제 아니면 만들지마가
> 아니라, 되도록 이렇게 만들어줘. 이렇게 못 만드는 문제는 기존 출제대로 만들면 됨". 그래서 필수 17항목은 번호도 내용도
> 건드리지 않고, 별도 층(`☆`)으로 18~20번을 붙였다.
>
> 이 개정과 함께 **v5를 유일한 실행용 프롬프트로 확정**하고 v4는 이력 보관용으로 돌렸으며, 옛 규칙으로
> 만든 퀴즈 24건(JSON·미리보기)은 `_보관_20260828_구규칙/`으로 옮기고 전량 재생성하기로 했다.

**대단원별 누적 CSV 구조 (v1.1 신설, `산출물/대단원별/`)**

128건 규모로 반복 처리하게 되면서, 원고 1건마다 개별 파일을 만드는 대신 **대단원 단위로 누적되는
CSV 파일**을 최종 산출물로 삼는다. `재료/Story전체 목차_0730.csv` 기준 대단원 9개(프롤로그, 1~7,
에필로그)마다 파일 하나씩, 목차의 전체 아티클(2026-08-28 기준 128개, 목차 갱신 시 재확인)을 스켈레톤(뼈대) 행
으로 미리 채워두고, 원고가 완료되는 대로 해당 아티클의 문제~정답해설 칸만 채워나간다. 빈 칸이 곧
"아직 처리 안 된 아티클" 표시를 겸하므로 별도 진행현황 파일은 두지 않는다.

- 파일명: `00_프롤로그_과학이란_무엇인가.csv` ~ `08_에필로그_우리의_미래.csv` (대단원 번호를 2자리로
  붙여 정렬 보장)
- 헤더(17열)와 스켈레톤 행 구조는 위 "퀴즈 출력 TSV — 17개 열" 표와 동일
- 각 아티클마다 7행(문항 1~7)을 미리 만들어두고, 분과·중단원·아티클은 1행에만, 문항 번호·난이도·
  퀴즈 유형은 기준 배열대로 채워 넣는다. 문제~정답해설(7개 열)은 원고 처리 전까지 빈칸으로 둔다
- 이 스켈레톤은 **기준 구조**이며, 기획자 선택이 기준 배열과 다르면 반영 시 **그 아티클 블록만**
  선택에 맞춰 고친다 (Step 6 참고). **행 수 7행과 문항 번호 1~7은 고정**이고, 아티클마다 달라질 수
  있는 것은 난이도·퀴즈 유형 배열이다

---

### 5. 출력 명세

- **사람 검토용**: 별도 파일로 저장하지 않는다 (v1.1 변경 — 128건 규모에서 원고마다 검토용 .md를
  쌓으면 산출물 폴더 관리가 어려워짐). `퀴즈생성자`가 문항 미리보기·자기검수 리포트·사람 확인 지점을
  응답으로 반환하면, 그 자리에서 사람이 검토하고 "확인했어"로 확정한다.
- **시트 반영용**: 원고별 CSV를 새로 만들지 않고, `산출물/대단원별/{대단원 파일}.csv`의 해당 아티클
  스켈레톤 행을 채우는 방식으로 통일한다. UTF-8 BOM 포함(Excel 한글 깨짐 방지, CLAUDE.md 규칙 5번).
  콤마 구분 CSV(콤마 포함 값은 따옴표 처리)를 표준으로 한다
- **위치**: `산출물/대단원별/`
- **파일명 규칙(확정, PLAN 오픈이슈 6 해결)**: `{대단원 2자리 번호}_{대단원명 축약}.csv`
  (예: `01_빅뱅_우주의_시작.csv`)

---

### 6. 규칙 및 예외 처리

| 상황 | 처리 규칙 |
|---|---|
| 재료에 원고 또는 프롬프트 템플릿이 없음 | 진행하지 않고 사용자에게 알림. 없는 자료를 가정해서 만들지 않음 |
| 원고에 없는 사실로만 답이 만들어짐 | 해당 문항 옆에 `[확인 필요: 본문 근거 부족]` 표시 |
| 목차(`Story전체 목차_0730.csv`)와 원고 내용이 매칭되지 않음 | 추정하지 않고 `[확인 필요]`로 표시(2장·Step2 참고). 목차 자체가 갱신되어 스켈레톤과 안 맞을 수 있으므로, 자주 매칭 실패하면 목차 갱신 여부를 사람에게 확인 |
| 자기검수 17항목 중 하나라도 미통과 | 결과물에서 어떤 항목이 왜 미통과인지 명시, 임의로 고쳐서 통과 처리하지 않음 |
| 지향 항목(18~20번) 미달성 | **미통과가 아니다.** 문항을 고치지 않고 `미달성 — 사유`만 적고 그대로 진행. 달성하려고 소재를 바꾸거나 원고에 없는 내용을 끌어오지 않음 |
| 사람 확인 지점 문구 | 어떤 경우에도 삭제·요약하지 않고 그대로, 결과물 맨 마지막에 포함 |
| 대단원별 CSV에서 대상 아티클을 못 찾음 | 임의로 새 행을 만들지 않고 사람에게 알림 (목차 변경 가능성 확인) |

---

### 7. 트리거 명세

| 트리거 | 조건 | 동작 |
|---|---|---|
| 수동 트리거 | 사용자가 재료 폴더에 원고를 추가하고 처리를 요청 | Step 1~6 실행 (한 번에 원고 1건) |

자동 폴더 감시나 정기 스케줄 트리거는 이 과제 범위에 포함하지 않는다 (PRD Non-goals 참고,
1인 개인 업무 자동화이며 아직 반복 처리 빈도가 검증되지 않았기 때문 — PLAN 오픈 이슈 참고).

**여러 원고 순차 처리 (v1.1 추가, PLAN 오픈이슈 3 해결)**: 원고 128건은 완성되는 대로 순차적으로
들어오며, 처리 순서는 항상 "그때그때 사용자가 처리를 요청한 원고" 기준이다 — 목차상의 순서와는
무관하다. 여러 원고가 재료 폴더에 동시에 있어도 한 번에 하나씩만 처리한다.
