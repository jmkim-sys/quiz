# 업무 자동화 과제 — 작업 규칙

기획 근거 문서 (자세한 배경·처리 로직·실행 계획은 아래 문서 참고):
- [PRD_요구사항정의서.md](./PRD_요구사항정의서.md) — 배경, 목표(G1~G3), Non-goals, User Story
- [SPEC_기능명세서.md](./SPEC_기능명세서.md) — 처리 로직 Step 1~6, 데이터 구조(TSV 17열), 규칙/예외
- [PLAN_실행계획서.md](./PLAN_실행계획서.md) — 실행 단계, 오픈 이슈, 리스크

## 과제정의

출처: `재료/워크시트C_PBL과제확정서_작성본 - 워크시트C.csv` (선택 업무: A-1 STORY 퀴즈
프롬프트·퀴즈 생성·검수 과정 개선 / 내부운영 / 프로젝트 반복(한시))

| 항목 | 내용 |
|---|---|
| 과제 한 줄 | STORY 원고를 넣으면 Lv1~Lv4 퀴즈 초안과 1차 검수 결과가 한 번에 나오게 한다. (자동화 구간 = 매번 새로 하는 '프롬프트 재작성' + '눈으로 하는 1차 검수') |
| 입력(재료) | ① STORY 원고 텍스트(노션·구글드라이브) ② 퀴즈 레벨 기준표 Lv1~Lv4(내부 문서) ③ 기존 퀴즈 프롬프트 템플릿 ④ 검수 반려 사유 로그(시트). 데이터 보안 LV1~2, 개인정보 없음 — 단 미발행 원고는 LV2로 취급 |
| 출력(결과물) | Lv1~Lv4 문항 + 정답·해설 + 자기검수 리포트(레벨 적합성/중복/모호성) |
| 성공 기준 | 1회차 검수 반려 건수 현재 대비 50% 이하 / 1건 소요시간 40분 → 15분 (주 3건×25분 절감 = 주 75분, 월 약 5시간) |
| 사람이 확인할 것 | 과학적 사실 오류, 정답 타당성, 최종 발행 승인. 그리고 미발행 원고는 외부 도구에 원문 저장 금지(세션 내 사용만) 준수 여부 |

이 폴더는 PBL(Project-Based Learning) 업무 자동화 과제 전용 작업 공간이다. 프로젝트 루트의
`CLAUDE.md`(회의록 기반 업무현황 자동화 과제)와는 별개이며, 이 폴더 안에서는 아래 규칙이 우선한다.

## 규칙

1. **워크시트C(PBL 과제 확정서)가 이 과제의 기준 문서다.**
   `재료/` 안의 워크시트C 파일이 요구사항·범위 판단의 근거가 된다. 다른 문서나 추측이 아니라
   워크시트C 내용을 기준으로 작업한다.

2. **원본 파일은 절대 수정하지 않는다. 결과는 항상 `산출물/` 폴더에 새 파일로 저장한다.**
   `재료/` 안의 파일(워크시트C 포함)은 읽기 전용으로만 취급한다. 가공·정리·변환 결과물은
   원본을 덮어쓰지 말고 `산출물/`에 새 파일명으로 만든다.

3. **원본에 없는 값을 지어내지 않는다.**
   워크시트C나 `재료/`의 다른 파일에 없는 값은 추측해서 채우지 않는다. 불확실하거나 근거가
   부족하면 `[확인 필요]`로 표시해 둔다.

4. **"확인했어"라는 말이 있기 전에는 다음 단계로 넘어가지 않는다.**
   각 단계의 결과를 사용자에게 제시한 뒤, 사용자가 명시적으로 "확인했어"라고 말하기 전까지는
   다음 단계 작업을 시작하지 않는다.

5. **CSV로 저장하는 산출물은 UTF-8 (BOM 포함)으로 저장한다.**
   BOM이 없으면 Excel 등에서 열었을 때 한글이 깨진다. TSV/Markdown은 해당 없음.

## 폴더 구조

```
재료/            입력. 워크시트C·원고·프롬프트 템플릿·Story전체 목차 CSV 등 원본 자료 (수정 금지)
산출물/          출력. 이 과제에서 생성하는 모든 결과 파일
산출물/퀴즈데이터/  원고 1건 = JSON 1개(7슬롯 × 1문항 = **7문항**). 아래 도구의 입력이 되는 원본
                (2026-09-15 변경 — 그전에는 7슬롯 × A안·B안 = 14문항이었다)
산출물/퀴즈CSV/   기획자 검수용. 아티클 1건 = CSV 1개(**7행**). 대단원별과 **완전히 같은 17열 형식**이며,
                기획자는 이 7문항을 검수한다 (2026-09-15 신설 — 미리보기 HTML 대체)
산출물/대단원별/  최종 결과물. 대단원(9개) 단위 누적 CSV. 목차 전체 아티클(128개)이 스켈레톤으로
                미리 채워져 있고, 원고가 완성되는 대로 해당 아티클 7행만 채워진다
산출물/퀴즈미리보기/  **2026-09-15 생성 중단.** 기존 12개 HTML은 참고용으로 남기되 새로 만들지 않는다
기록/            HISTORY.md(진행 로그), LESSONS.md(막힌 점·주의사항), 진행상황.md(재개용 현황판)
tools/           Google Sheets 반영. `build_quiz_update.js`(검수용 CSV → `quiz_update.json`) +
                `update_google_sheet.py`(Apps Script 웹앱으로 전송). 접속 정보는 `.env`(git 제외)
_보관_20260828_구규칙/  2026-08-28 공통 지침 이전 규칙으로 만든 퀴즈 24건(JSON·미리보기).
                참고용이며 이 문항을 CSV에 반영하지 않는다. `산출물/`이 아니므로 검토·집계 대상 아님
_보관_20260915_AB14문항/  2026-09-15 7문항 전환 전의 A/B 14문항 원본(퀴즈데이터 12·퀴즈CSV 12).
                같은 취급 — 참고용이며 `산출물/`이 아니므로 검토·집계 대상 아님
```

## 기록 규칙

- `기록/HISTORY.md`: 한 일을 한 줄씩 시간순으로 누적한다. 기존 줄은 지우지 않는다.
- `기록/LESSONS.md`: 막혔던 것, 다음에 조심할 것을 적는다. 문제를 겪을 때마다 갱신한다.
- `기록/진행상황.md`: **세션이 끊겨도 이어할 수 있게 하는 현황판.** 세션을 시작하면 이 파일부터
  읽고, 원고 1건을 처리하거나 CSV에 반영할 때마다(= 중간중간) 상태 표를 갱신한다. HISTORY가
  "한 일의 이력"이라면 이 파일은 "지금 상태와 다음에 할 일"이며, 둘 다 갱신한다.

## 서브에이전트

호출 규칙만 명시한다 (역할·판단 기준 상세는 각 `.claude/agents/*.md` 참고).

| 이름 | 호출 시점 | 모델 |
|---|---|---|
| `퀴즈생성자` | 원고 1건을 퀴즈로 처리해야 할 때 (목차 자동 매칭 + 문항 생성 + 자기검수, 파일 저장은 안 함) | **Opus** |
| `대단원누적자` | `퀴즈생성자` 결과를 사용자가 "확인했어"로 확정한 뒤, `산출물/대단원별/` CSV에 실제 반영할 때 | **Sonnet** |
| `검토자` | 페이즈(단계) 단위 개발, 또는 사용자가 지시한 개발 작업이 끝날 때마다 | **반복 작업 Sonnet / 마일스톤 Opus** (라우팅 규칙 3-1) |

### 모델 선택 규칙 (2026-09-11 확정 — 사용자 지시 "sonnet과 opus 2개를 적절히 사용해서 토큰 사용량을 최대한 줄여")

Agent 호출 시 `model` 파라미터로 지정한다. **적지 않으면 전부 Opus로 돌아 토큰이 그대로 나간다.**

- **`퀴즈생성자` = Opus (내리지 않는다).** 문항을 **쓰는** 단계이고, 여기서 난 오류는 JSON·CSV로
  그대로 번져 재생성(20분+)과 기획자 재검수를 부른다. 규칙 3번(지어내지 않는다)·규칙 4번(순서 근거)
  판단이 전부 이 단계에서 일어난다. 가장 비싸지만 **가장 내리면 안 되는 자리**다.
- **`대단원누적자` = Sonnet.** 확정된 값을 CSV 칸에 **옮겨 적는** 작업이라 판단이 거의 없다. 게다가
  반영 뒤 원본 JSON과 전 칸 대조를 하므로 실수가 그 자리에서 걸린다.
- **`검토자` = 반복 작업이면 Sonnet, 마일스톤이면 Opus.** 라우팅 규칙 3의 두 단계에 그대로 얹는다.
  ⚠️ **다만 `검토자`는 품질 안전망이라 내리는 데 대가가 있다** — 2026-09-11에 Opus 검토자가 잡은
  "개념적 전제 관계 ≠ 사건의 순서"(4B 15번)는 미묘한 논리 구분이었고 Sonnet이 놓칠 수 있는 종류다.
  이를 메우는 장치는 **검토자 결과를 그대로 믿지 않고 지적된 자리를 내가 원고로 직접 확인하는 것**이다
  (규칙 4번 처리 절차와 같다). 검토자가 "통과"만 냈을 때도 **순서배열 4벌의 인접 교환 근거만은 직접
  본다** — 놓침(false negative)은 확인으로 메워지지 않기 때문이다.
- 판단이 서면 **품질 쪽으로 기운다.** 토큰 절감은 목표지만, 잘못 만든 문항을 다시 만드는 비용이
  Opus 한 번보다 크다(2026-09-10 실측: 검토자 1회 ≈ 원고 2~3건).

### 라우팅 규칙

1. 원고 1건 처리 요청 → `퀴즈생성자` 호출 → 결과(문항 내용·자기검수 필수 20항목[1~17 · 21~23] + 지향 3항목·
   사람 확인 지점)를
   사용자에게 제시 → 사용자의 "확인했어" 응답 후에만 `대단원누적자`를 호출해 실제 CSV에 반영한다
   (규칙 4번과 연결). `퀴즈생성자`는 파일을 저장하지 않으므로 이 순서를 건너뛸 수 없다.
   **반영이 끝나면 `node "산출물/도구/build-dashboard.js"`로 대시보드를 갱신한다**
   (2026-09-16 추가).
   **원고 1건 = 7문항이고, 그 7문항이 그대로 대단원별 CSV의 7행이 된다** (2026-09-15 확정 —
   사용자 지시 *"앞으로 14문제 말고 7문제만 만들어 원고 한 개당 7문항만 나오게"*). 문항 번호는
   1~7, 난이도·유형은 기준 배열 그대로이므로 **행 구조를 고칠 일이 없다.**
   ⚠️ 옛 A/B 2안 경로(14문항을 만들어 기획자가 7개를 고르면 그 선택에 맞춰 행 구조를 고치던 절차,
   2026-08-31 확정)는 **적용 중지**다. 2026-09-15 이전에 그렇게 반영된 아티클
   (`01_빅뱅_B_우주론` 등)의 기록을 읽을 때만 필요한 배경이다.
2. 페이즈 단위 개발이 끝나거나 사용자가 지시한 개발 작업이 끝나면, 사용자의 "확인했어" 응답을 받기
   전에 반드시 `검토자`를 호출한다.
3. **`검토자` 호출 규모는 두 가지다 (2026-09-10 확정 — 토큰 실측에 따른 변경).**
   - **원고 1건 처리 같은 반복 작업 → `검토자` 1명.** 두 관점(① 원고 대조 = 빠진 것·지어낸 것
     ② 규칙·SPEC 표기 정합성)을 **한 프롬프트에 합쳐** 맡긴다. 검토 대상은 "이번에 새로 생성·갱신된
     파일"로 좁힌다.
   - **대단원 완료·규칙 개정·도구 변경 같은 마일스톤 → 관점을 달리해 2~3개 병렬.** 이때만 전체
     재검토를 한다. 병렬 결과는 모두 취합해 사용자에게 함께 제시한다.

   근거: 2026-09-10 실측으로 **검토자가 서브에이전트 토큰의 69%**를 썼다(원고 2건 693,342토큰 중
   479,296). 1명으로 줄이면 검토 비용이 절반이 되고 건당 약 350K → 약 230K가 된다. 두 관점을 합쳐도
   그날 잡힌 큰 지적 2건(원고에 없는 표현 유입 · 순서배열 규칙 4번 위반)은 한 명이 볼 수 있는
   종류였다. 자세한 수치는 `기록/HISTORY.md` 2026-09-10 항목.
4. 병렬 검토 결과 중 하나라도 개선사항(빠진 것 / 지어낸 것으로 보이는 것 / 규칙 위반)이 발견되면,
   `기록/LESSONS.md`에 "무엇이 왜 발생했는지 + 재발 방지 방법"을 한 항목으로 추가해 재발을 방지한다.
   전원 "통과"면 LESSONS.md는 갱신하지 않는다.

## 퀴즈 문항 규칙 (호출할 때 다시 적지 않아도 된다)

> ⚠️ **2026-09-15 — 규칙 1번·7번은 적용 중지다.** 둘 다 A안·B안 2안을 전제로 한 규칙인데
> 2안 생성을 그만두었기 때문이다(원고 1건 = 7문항). **번호는 다시 매기지 않는다** —
> `퀴즈생성자.md`·`검토자.md`·`SPEC_기능명세서.md`가 같은 번호로 서로를 가리키고 있어,
> 번호를 당기면 옛 기록이 전부 어긋난다. 2~6·8·9번은 그대로 살아 있는 규칙이다.

아래 열두 규칙 중 **1~8번·10~12번**은 `퀴즈생성_프롬프트_템플릿_v5.md`, `퀴즈_AB_2안_생성기준.md`(보관),
`SPEC_기능명세서.md` 자기검수 7·8·15·16·17번, 그리고 `.claude/agents/퀴즈생성자.md`·`검토자.md`에
**모두 반영되어 있다.** **9번은 화면 표시 규칙이라 문항을 만들 때 할 일이 없으므로**
`퀴즈생성자.md`에는 없다 (2026-09-01 정정 — 그전에는 "아홉 규칙 모두 반영"이라고 적혀 있었으나
사실이 아니었다). ⚠️ **2026-09-15부터 9번은 검토 대상이 없다** — 이 규칙을 구현하던
`산출물/도구/미리보기_템플릿.html`로 미리보기를 더 이상 만들지 않기 때문이다. 데이터 쪽 규칙
(예문 없는 칸에 `-`)만 CSV에 그대로 살아 있고, 그건 검토자가 CSV에서 확인한다.
`퀴즈생성자`를 부를 때 프롬프트에 옮겨 적을 필요가 없다.
1~3번은 2026-08-15, 4~9번은 2026-08-28에 사람 검토로 확정되었고,
6번의 빈칸 표기(`○` → 밑줄 `_`, 띄어쓰기 반영)는 2026-08-31에 다시 확정되었다.
**10~12번은 2026-09-17에 추가되었고**(과학자 표기 통일 · 본문 노출 재검토 · 정답 번호 분산),
`퀴즈생성_프롬프트_템플릿_v5.md`의 「공통 작성 원칙」과 자기검수 **21·22·23번**에 반영되어 있다.

> **실행용 프롬프트는 `퀴즈생성_프롬프트_템플릿_v5.md` 하나다** (2026-08-28 확정).
> `v4.md`는 v3 → v4 변경 이력을 남긴 보관용이며 4~7번이 빠져 있으므로 **읽지도, 근거로 인용하지도
> 않는다.**

1. ~~**두 안은 서로 다른 내용을 묻거나, 같은 개념이라도 다른 방식으로 묻는다.**~~
   **⛔ 2026-09-15 적용 중지 — A/B 2안을 만들지 않는다.** 원고 1건에서 7문항만 내므로 "두 안"이
   없다. (2026-08-15 확정 → 2026-08-28 개정 → 2026-09-15 중지. 그 이전 아티클의 A/B 문항을
   읽을 때만 필요한 배경이다.)
2. **직접 입력(5번) 정답은 띄어쓰기가 있어도 된다.** 채점이 공백을 무시한다. 원고 표기를 임의로
   붙여 쓰지 않는다. 빈칸 표기의 밑줄 개수는 공백을 뺀 글자 수와 같고, **띄어쓰기는 표기에 그대로
   살린다**(규칙 6번). (옛 규칙 "띄어쓰기 없는 단일 단어"는 폐기)
3. **순서배열 문장은 뒤섞인 상태에서 각각 단독으로 읽혀야 한다.** 문장 첫머리에 다른 문장을 가리키는
   지시어나 순서를 드러내는 접속어를 쓰지 않는다 — 정답을 흘리기 때문이다. 인과는 내용으로 드러낸다.
4. **순서배열은 순서를 정하는 근거가 있을 때만 만든다.** 근거는 둘 중 하나여야 한다 —
   ① **원고에 표시된 연도**(기본) ② 연도가 없어도 **이론적으로 순서가 필연적인 경우**(앞 문장이 뒤
   문장의 전제가 되어 뒤바꾸면 성립하지 않음). **원고의 문장 순서만으로는 만들지 않는다** — 과학을
   이해한 학습자도 풀 수 없고 원고를 외운 사람만 맞히기 때문이다. 근거가 없으면 소재를 바꾸고, 그래도
   없으면 억지로 만들지 말고 자기검수 15번에 그대로 보고한다.
5. **비유적·은유적 표현을 정답 칸·보기 칸에 넣지 않는다.** (예: `결합 손`) 원고의 비유는 원고 안에서만
   통하는 말이지 학습자가 익혀야 할 과학 용어가 아니다. **순서배열 문장도 보기 칸이므로 해당된다.**
   [문제]·[예문]에서 맥락으로 인용하는 것은 괜찮다. 원고에 정식 용어 없이 비유만 있으면 그 소재로는
   문항을 만들지 않는다 (규칙 3번 "지어내지 않는다"와 연결).
6. **직접 입력(5번)은 발문을 고정하고 빈칸을 예문에 둔다.**
   [문제] 칸 = `빈칸에 들어갈 알맞은 말을 쓰세요.` 한 문장. [예문] 칸 = 정답을 유추할 수 있는 설명 +
   **정답을 글자 단위로 밑줄 `_`로 바꾸고 띄어쓰기는 그대로 살린** 빈칸을 포함한 완결된 문장.
   빈칸에는 **핵심 용어 하나만** 들어가고 문맥상 **정답이 하나로 특정**되어야 한다.
   `라니냐` → `___` · `금속 결합` → `__ __` · `태양 중성미자 문제` → `__ ____ __`.
   문자는 **밑줄 `_`(U+005F)** 이며, 이 표기 하나로 "빈칸"·"글자 수"·"띄어쓰기"를 함께 나타낸다.
   채점은 공백을 무시하므로 학습자가 붙여 써도 정답이다(규칙 2번) — 표기가 띄어쓰기를 보여주는 것과
   채점이 공백을 무시하는 것은 별개다. (2026-08-31 확정. 옛 표기 `○` 반복과 `[ 빈칸 ](N글자)`,
   옛 규칙 "빈칸을 [문제] 칸에 둔다"는 모두 폐기)
7. ~~**A안·B안은 서로 다른 핵심 키워드 또는 학습 포인트로 만든다.**~~
   **⛔ 2026-09-15 적용 중지 — A/B 2안을 만들지 않는다.** 다만 **"원고 내 핵심 키워드의 중요도를
   유지하는 범위 안에서 소재를 고른다"**는 부분은 7문항 단일안에도 그대로 살아 있다 — 3↔6번,
   4↔7번의 소재를 가를 때 비중이 작은 곁가지를 끌어오지 않는다(자기검수 4번).
8. **순서배열 보기 라벨은 `가`·`나`·`다`·`라`다.** (2026-08-28 확정) 보기1=가 … 보기4=라이며, 정답1은
   라벨을 하이픈으로 이은 `나-라-가-다` 형태다. 숫자 `2-4-1-3`은 옛 형식이다. 알파벳 A~D를 쓰지 않는
   이유는 이 과제가 **A안·B안**으로 2안을 관리하던 때 "4A의 정답은 B-D-A-C"처럼 섞였기 때문이다
   (2안은 2026-09-15에 그만두었지만, 라벨 규칙은 그대로 유지한다 — 기존 문항과 표기를 맞춘다).
   **발문에서는 이것을 '문장'이라고 부른다** — (X) `다음 카드는 ~` → (O) `다음 문장은 ~`.
   '카드'는 화면 구현상의 이름이지 학습자가 읽을 말이 아니다.
9. **지문(예문)이 없는 문항은 화면에 지문 박스를 그리지 않는다.** (2026-08-28 확정) 17열 데이터에서는
   예문 없는 칸에 `-`를 넣는 규칙이 그대로 유지되며(`X`는 OX 정답과 혼동됨), **표시만 감춘다.**
   문항을 만들 때 따로 할 일은 없다.
   ⚠️ **2026-09-15 — 이 규칙의 "표시" 부분은 당분간 적용될 곳이 없다.** 이를 구현하던 미리보기
   HTML을 만들지 않기로 했기 때문이다(「퀴즈 산출물 저장 규칙」 참고). **데이터 쪽 `-` 규칙은
   그대로 지킨다** — 나중에 실제 화면에 실을 때 필요한 값이라서다. 규칙 자체를 폐기한 것이 아니다.

10. **과학자 표기는 칸에 따라 정한다 — 지문은 풀네임, 보기·정답은 한 단어.** (2026-09-17 확정 —
    사용자 지시 *"생성 시 과학자 이름 쓰다가, 성 쓰다가 하지말고 이름만 쓰도록 통일하자"* →
    *"문제 보기에만 이름으로 표기하자 문제에는 성까지 붙여야겠다"*)
    - **[문제]·[예문]·[정답해설] = 성까지 붙인 풀네임.** `요하네스 케플러` · `베라 루빈` ·
      `갈릴레오 갈릴레이`. 학습자가 읽는 지문이므로 인물을 정식으로 부른다.
    - **보기 칸·정답 칸 = 한 단어.** `케플러` · `루빈` · `갈릴레이`.
      **순서배열 문장 4개도 보기 칸이므로 한 단어**다(규칙 5번과 적용 범위가 같다).
    - ⚠️ **풀네임은 원고에 있을 때만 쓴다.** 원고가 성 없이 한 단어로만 부르는 인물은 [문제]에서도
      한 단어 그대로다 — 없는 이름을 지어내지 않는다(규칙 3번).
    - ⚠️ **한 칸 안에서 표기를 섞지 않는다.** 실제로 `00_프롤로그_A_자연과학소개`의 순서배열 한
      문항이 `요하네스 케플러가`와 `케플러는`을 함께 쓰고 있었고, 이 규칙은 그것을 막으려는 것이다.
    - **소재**: 과학자 이름 자체를 정답으로 묻는 문항은 만들지 않는다. 원고가 인물·발견의 역사
      자체를 주제로 삼아 이름이 핵심 학습 내용일 때만 예외로 하고, 그 사유를 자기검수 21번에 적는다.
    - ⚠️ **[예문]·[정답해설]을 지문으로 묶은 것은 해석이다** — 사용자 지시는 [문제]와 보기만
      언급했다. 둘 다 학습자가 읽는 서술이라 [문제] 쪽에 붙였다. 다르게 정하면 여기부터 고친다.
11. **문제·예문만 읽고 답이 나오는 문항은 다시 검토한다.** (2026-09-17 확정 · 자기검수 22번)
    정답 용어나 그 정의가 발문·예문·보기에 **글자 그대로** 드러나 있으면 학습자는 개념을 몰라도
    맞힌다. 출력 전에 정답을 모르는 사람이 되어 [문제]·[예문]만 읽어보고 답이 짚이는지 확인한다.
    ⚠️ **직접 입력(5번)은 대상이 아니다** — 예문이 정답을 유추하게 하는 것이 그 유형의 설계다
    (규칙 6번). 지향 1번(설명을 먼저 주고 이름을 묻는다)과도 구분한다 — 설명으로 **추론**하게
    하는 것은 권장이고, 정답 용어를 **그대로 노출**하는 것이 금지다.
12. **정답 번호가 한쪽으로 몰리지 않게 한다.** (2026-09-17 확정 · 자기검수 23번)
    번호가 걸리는 자리는 1번 인라인의 두 빈칸·3번 사지선다 단수·6번 사지선다 복수다. 7문항을
    만든 뒤 정답 번호를 한 줄로 늘어놓고 같은 번호만 반복되지 않는지 본다. 고칠 때는 보기 순서를
    바꾼다. ⚠️ **번호를 나누려고 자연스러운 배열(크기순·시간순 등)을 깨지 않는다** — 그때는
    그대로 두고 자기검수 23번에 사유를 적으며, 사유가 있으면 통과로 본다.

## 퀴즈 문항 지향 원리 — 규칙이 아니라 지향점 (2026-09-01 확정)

위 열두 규칙은 **반드시 지킬 것**이고, 아래 셋은 **되도록 그렇게 만들면 좋은 방향**이다.
사용자 지시(원문 그대로): *"이런 문제 아니면 만들지마가 아니라, 되도록 이렇게 만들어줘. 이렇게 못
만드는 문제는 기존 출제대로 만들면 됨"* 근거는 2026-08-31~09-01 기획자 검수에서 **모범 문항(별표)으로
표시된 8건**(5개 아티클)의 공통 특징이며, 8건 중 5건이 1번 인라인이었다.

1. **설명을 먼저 주고 이름을 묻는다.** 정답이 될 용어의 정의·조건·현상을 [예문]이 서술하고 학습자는
   그 이름을 답한다. 원고를 외우지 않아도 이해했으면 풀린다.
2. **수치는 해설로 보내고 보기에서는 관계를 묻는다.** 퍼센트·측정값을 그대로 맞히게 하는 대신
   크기 비교·순서로 바꾸고, 실제 수치는 정답해설에서 알려준다.
3. **오답은 같은 범주의 진짜 개념으로 만든다.** 오답 하나하나가 학습자가 실제로 가질 법한 오개념을
   대표하게 한다. 읽자마자 걸러지는 보기는 변별력을 깎는다.

**미달성은 미통과가 아니다.** 자기검수 필수 항목(1~17 · 21~23)은 그대로이고, 지향은 18~20번으로 따로 묶어
`달성`/`해당 없음`/`미달성 — 사유`로만 보고한다. **지향을 맞추려고 소재를 비틀거나 원고에 없는
내용을 끌어오면 그쪽이 규칙 위반**이다(규칙 3번). 상세 사례는
`퀴즈생성_프롬프트_템플릿_v5.md`의 「☆ 모범 문항 지향 원리」와 「부록. 모범 문항 예시」에 있다.

## 퀴즈 산출물 저장 규칙

> **2026-09-15 변경 ② — 원고 1건 = 7문항이다. A안·B안 2안을 만들지 않는다.** 사용자 지시:
> *"앞으로 14문제 말고 7문제만 만들어 원고 한 개당 7문항만 나오게"*
> - `퀴즈생성자`는 자리마다 **문항 하나씩, 7문항**을 낸다. 기획자가 고르는 단계가 없어졌으므로
>   **그 7문항이 곧 확정 문항**이고, 대단원별 CSV의 7행에 1~7번으로 그대로 들어간다.
> - JSON도 슬롯 안에 `A`·`B`를 두지 않고 **문항 내용을 슬롯에 바로** 담는다
>   (`{ no, lv, type, q, ex, sol, <유형별 데이터> }`). 검수용 CSV는 **7행**이고 `문항 번호`는 1~7이다.
> - **기존 12건도 2026-09-15에 7문항으로 줄여 다시 냈다** — CSV 반영이 끝난 5건은 기획자가 확정한
>   7개를, 선택 대기였던 7건은 `기록/진행상황.md`에 적힌 "권장" 조합(순서배열 근거가 있는 쪽)을
>   남겼다. 어느 안에서 왔는지는 각 JSON `meta.축약`에 적혀 있다.
> - `퀴즈_AB_2안_생성기준.md`는 **보관용**이 되었다. 새로 만들 때 읽지 않는다.

> **2026-09-15 변경 ① — 검수용 산출물은 HTML 미리보기가 아니라 CSV다.** 사용자 지시:
> *"앞으로 퀴즈 미리보기 생성 시, html 대시보기 미리보기가 아닌 그냥 퀴즈내용 텍스트로만 복사해서
> 엑셀파일로 만들어"* + *"산출물\대단원별 폴더에 있는 파일들처럼 만들어. 걔네만 만들면 돼 앞으로"*
> 근거는 토큰 실측이다 — 미리보기 생성 자체는 로컬 node 스크립트라 토큰이 0에 가깝지만,
> **`검토자`가 생성물 HTML을 Read할 때 건당 약 23,500~24,800토큰**이 들었고 그중 6.6만 자는
> 매번 똑같은 템플릿(CSS·JS)이었다. 같은 내용을 17열 CSV로 주면 약 5,500~6,700토큰이라
> **건당 약 18,000토큰(건당 총량의 5~8%)** 이 줄어든다. 수치는 `기록/HISTORY.md` 2026-09-15 항목.

- `퀴즈생성자`는 파일을 쓰지 못하므로, **결과를 받은 쪽이 즉시** `산출물/퀴즈데이터/<이름>.json`으로
  저장한다. 저장하지 않으면 세션이 끊길 때 문항이 통째로 사라진다. **JSON은 계속 만든다** —
  아래 CSV 생성기의 입력이자, 정답 번호 오타를 걸러 내는 안전장치가 여기에 걸려 있다.
- **저장 직후 구조를 검증한다**: `node "산출물/도구/퀴즈JSON검증.js"`
  손으로 옮겨 적는 단계가 있어 정답 번호 오타가 나면 CSV에 틀린 답이 정답으로 실린다.
  (구조만 본다 — 정답이 원고에 맞는지는 CSV를 보고 사람이 확인한다.)
- **검수용 CSV는 손으로 쓰지 않고 생성기로 만든다**:
  `node "산출물/도구/퀴즈CSV생성.js" "산출물/퀴즈데이터/<이름>.json"`
  → `산출물/퀴즈CSV/<이름>.csv` (7행 · 17열 · UTF-8 BOM · CRLF)
  **형식은 `산출물/대단원별/*.csv`와 완전히 같다.** 헤더 17열이 같고, `분과`·`중단원`·`아티클`은
  첫 행에만 적으며, 표기 규칙(OX 정답은 라벨 `O`/`X` · 순서배열 정답은 `다-나-라-가` ·
  직접 입력은 보기 1~6 전부 빈칸 · 예문 없으면 `-`)도 같다. 기준은 기획자 승인본
  `산출물/대단원별/퀴즈샘플_260831.csv`이며, 2026-09-15에 그 7문항과 대조해 6문항이 13칸 전부
  일치함을 확인했다(나머지 1건은 기획자가 검수 중 보기를 손으로 고친 것).
  **2026-09-15 7문항 전환 뒤로는 행 수(7행)와 `문항 번호`(1~7)까지 같아져, 이 CSV가 곧 대단원별
  CSV에 들어갈 블록 그대로다.** 기획자가 검수해 "확인했어"라고 하면 `대단원누적자`가 그대로 옮긴다.
  (옛 형식은 14행에 `문항 번호`가 `A1`~`A7`·`B1`~`B7`이었고, 기획자가 그중 7개를 골랐다.)
- **미리보기 HTML(`미리보기생성.js`)은 더 이상 만들지 않는다.** 이 도구와
  `미리보기_템플릿.html`은 지우지 않고 남겨 두었으나 **정기 절차에서 빠졌다.**
  사용자가 명시적으로 요청할 때만 예외로 돌린다.
- **대시보드(`build-dashboard.js`)는 2026-09-16에 정기 절차로 돌아왔다.** 사용자 지시:
  *"지금부터 만든 퀴즈 퀴즈 현황 대시보드에 반영하자"* — `대단원누적자` 반영이 끝날 때마다
  `node "산출물/도구/build-dashboard.js"`를 돌려 `산출물/퀴즈제작현황_대시보드_v2.html`을 갱신한다.
  같은 날 **미리보기 HTML을 읽던 자리를 전부 `산출물/퀴즈CSV/*.csv`로 바꿨다** — 그러지 않으면
  KPI·링크·동기화 검사 두 가지가 조용히 멈춘 채 옛 수치를 보여준다. 경위는 `기록/HISTORY.md`
  2026-09-16 항목.
  ⚠️ **대시보드의 집계 원본은 로컬 파일이다 — 팀 Google 시트가 아니다.** 2026-09-16부터
  **만든 문항 수는 `산출물/퀴즈CSV/*.csv`**(퀴즈를 만들면 바로 생기는 파일)에서 읽고,
  전체 규모·아티클명·분과는 `산출물/대단원별/*.csv`에서 읽는다.
  2026-09-16에 사용자가 *"로컬로 처리하는게 맞을듯"* 으로 정했다. 따라서 대시보드 수치는 **"우리가
  만든 양"이지 "시트에 올라간 양"이 아니다** — 둘은 어긋날 수 있다(2026-09-16 기준 로컬 6건 · 시트 2건).
- 원고는 `재료/최종 원고_텍스트변환/`의 변환본을 쓴다. `재료/최종 원고/`의 `.docx` 원본은 바이너리라
  `퀴즈생성자`(Read/Glob/Grep만 보유)가 열 수 없다.
- **5번(직접 입력) 슬롯에는 `exFirst`를 넣지 않는다.** 발문(`빈칸에 들어갈 알맞은 말을 쓰세요.`)이
  먼저 보이고 예문 박스가 뒤에 와야 하는데, `exFirst: true`면 순서가 뒤집힌다. 위 문항 규칙 6번과 연결.
