# LESSONS

- 재료 폴더가 비어 보여도 사용자가 방금 넣었을 수 있어서, "없다"고 단정하기 전에 다시 확인하거나
  사용자에게 "더 넣을 자료가 있는지"부터 물어보는 게 안전하다. 그렇게 안 하고 바로 추측으로
  초안(01_퀴즈생성_프롬프트_템플릿_v1.md, Lv1~4 방식)을 만들었다가, 나중에 실제 자료(7문항 고정
  배열, TSV 17열 형식)가 도착하면서 형식이 완전히 달라 다시 만들어야 했다.
- 재료의 실제 프롬프트 템플릿에 이미 "천문/지구환경 분과는 지엽적인 수치 암기보다 스케일·인과
  관계를 물어라"는 규칙이 있었는데도, 첫 문항을 "몇 년도" 빈칸 채우기로 만들어서 그 규칙을 스스로
  어겼다. 다음엔 템플릿에 이미 있는 분과별 가이드·설계 원리(Test-Enhanced Learning 등)를 문항
  하나하나 만들 때마다 다시 대조해서, 규칙과 어긋나는 문항이 없는지 스스로 점검하고 낸다.
- CSV 산출물을 UTF-8(BOM 없음)로 저장했더니 Excel에서 열었을 때 한글이 특수문자로 깨졌다.
  원인은 원고_1.txt의 CP949 문제와는 다른, "BOM 없는 UTF-8을 Excel이 시스템 기본 인코딩(한국어
  Windows는 CP949)으로 잘못 해석"하는 문제였다. UTF-8 BOM(EF BB BF)을 파일 맨 앞에 붙이는
  방식으로 해결. 앞으로 CSV로 저장하는 산출물은 항상 BOM을 포함해서 저장한다 (CLAUDE.md 규칙
  5번에 반영함). 애초에 루트 프로젝트 CLAUDE.md의 log.csv 규칙에 이미 같은 주의사항이 적혀
  있었는데, 이 폴더 규칙에는 없어서 처음엔 놓쳤다.
- 재료의 iKAOS 프롬프트 템플릿 문서에 적힌 인라인 정답 표기 규칙("정답1=빈칸[1], 정답2=빈칸[2]")과
  팀이 실제로 시트에 기입하는 방식("정답1 한 칸에 쉼표로 구분해서 모두 적음")이 서로 달랐다.
  문서화된 템플릿 규칙이라고 해서 실제 운영 관례와 항상 같지는 않으므로, 애매하면 사람에게
  실제 사용 방식을 먼저 확인하는 게 안전하다. 또한 사용자가 "산출물"을 검토 중일 때 파일을
  덮어쓰려 하면 Excel 등에서 열려 있어 잠겨 있을 수 있다 — 그럴 땐 실패로 끝내지 말고 새 버전
  파일명으로 저장한다. **(2026-08-07 보강)** 이 항목이 단정한 "팀 관행 = 정답1에 쉼표로 합침"은
  이후 뒤집혔다 — 최종 확정값은 아래 "인라인 정답 표기 규칙이 …" 항목의 **정답1·정답2 분리**다.
  이 항목은 당시 경위 기록으로만 읽을 것.
- 검토자를 관점 다르게(누락/지어낸 값/SPEC 정합성) 3개 병렬 호출해보니, 단일 검토자 1회 실행보다
  더 많은 문제를 잡아냈다. 특히 "사람 확인 지점" 문구는 SPEC 원문을 그대로 복사하지 않고 제목+
  불릿으로 재구성했고, 위치도 파일 맨 아래가 아니라 중간(뒤에 문항 미리보기·CSV 안내 등이 더
  이어짐)이었다 — SPEC Step5 "결과물 맨 아래에 그대로 포함, 삭제·수정 금지"를 두 파일(v1.md,
  v2.md) 모두에서 어긴 상태다. 다음부터는 사람 확인 지점을 작성할 때 SPEC 문구를 그대로
  복사-붙여넣기하고 파일 맨 마지막 줄에 두며, 그 뒤에 다른 섹션을 추가하지 않는다.
- SPEC 4장에서 17번째 열 이름을 "정답해설"로 확정했는데도, 이후 만든 퀴즈생성_프롬프트_템플릿_v4.md와
  거기서 파생된 퀴즈결과_원고_1_v1.md 일부에 "정답피드백"/"정답 피드백"으로 다르게 표기된 채 남아있었다.
  새 버전 파일을 만들 때 SPEC의 확정 명칭을 다시 grep으로 대조하지 않고 손으로 옮겨 적어 생긴
  오타성 불일치로 보인다. 앞으로 열 이름처럼 SPEC에 확정값이 있는 항목은 복사-붙여넣기하고,
  검토자 호출 시 SPEC 정합성 관점을 포함시켜 이런 불일치를 잡는다.
- 자기검수 체크리스트를 10항목(PRD/SPEC 확정값)에서 11항목("단순 연도 암기 방지" 추가)으로
  늘렸는데, 그 변경을 PRD_요구사항정의서.md·SPEC_기능명세서.md에는 반영하지 않아 산출물과 기준
  문서가 서로 다른 숫자를 말하는 상태가 됐다. 산출물의 로직(체크리스트 항목 수 등)을 바꿀 때는
  같은 타이밍에 PRD/SPEC도 함께 갱신해서 "기준 문서 vs 실제 산출물" 불일치가 남지 않게 한다.
- CLAUDE.md 과제정의 표의 "사람이 확인할 것" 항목 중 "미발행 원고는 외부 도구에 원문 저장 금지
  (세션 내 사용만) 준수 여부"가 실제 산출물(v1.md·v2.md)의 "사람 확인 지점" 섹션에는 반영되지
  않았다. 사람 확인 지점을 작성할 때 SPEC Step5 문구뿐 아니라 CLAUDE.md 과제정의 표의 "사람이
  확인할 것" 행도 함께 체크리스트로 대조해야 빠뜨리지 않는다.
- HISTORY.md에 "템플릿·SPEC·산출물 모두 재반영했다"고 적어둔 변경이 실제로는 파일에 안 들어간
  사례가 두 건 발견됐다. (a) 인라인 보기를 "보기1·보기2 칸에 슬래시로 묶어 기입"으로 바꾼 변경이
  퀴즈생성_프롬프트_템플릿_v4.md에는 반영되지 않아, SPEC과 정반대인 낡은 규칙("보기1~3 = 빈칸[1],
  보기4~6 = 빈칸[2]")이 그대로 남아 있었다(HISTORY 17번 줄은 반영했다고 기록). (b) "정답피드백
  → 정답해설" 표기 통일도 템플릿 3곳(작성 규칙 제목, 17열 목록, 자기검수 10번)에 미반영 상태였다
  (HISTORY 30번 줄은 통일했다고 기록). 결과물이 틀리지 않은 건 퀴즈생성자가 템플릿과 SPEC을 함께
  읽었기 때문일 뿐, 템플릿만 보고 작업하면 틀리는 상태였다. 앞으로 "여러 파일에 반영"하는 변경은
  HISTORY에 기록하기 전에 파일별로 grep으로 반영 여부를 확인하고, 확인된 파일만 기록에 적는다.
- 인라인 정답 표기 규칙이 "정답1에 쉼표로 합치기" ↔ "정답1·정답2로 분리" 사이에서 **두 번 바뀌고
  한 번 재확인됐다** (HISTORY 14번 줄 쉼표 결합 → 19번 줄 분리 복귀 → 2026-08-06 분리 유지 재확인.
  세 번째는 논의만 있었고 규칙은 바뀌지 않았다). 2026-08-06 사용자 최종 확정: **정답 1칸에 정답 1개 —
  정답1 = 빈칸[1]의 정답 번호, 정답2 = 빈칸[2]의 정답 번호(분리)**. 보기는 보기1·보기2 칸에
  슬래시로 묶어 기입하는 방식과 함께 쓴다. 이 조합이 최종이며, 다시 바꾸자는 논의가 나오면
  문서 간 대조만 하지 말고 팀 시트에 실제로 기입된 예시부터 확인한다.
- 표기 하나를 정정할 때 "한 줄만 고치면 된다"고 판단·보고했는데, 검토자가 같은 문서 안에서 같은
  표기 4곳(SPEC 4장 107·118·119·129행의 `사지선다(단수)`류)을 더 찾아냈다. 원인은 문제를 발견한
  줄만 보고 같은 파일 전체를 grep하지 않은 것. 앞으로 표기·명칭을 정정할 때는 **수정 범위를 사용자
  에게 말하기 전에 해당 문자열을 프로젝트 전체 grep으로 세어보고**, "몇 개 파일 몇 곳"을 근거와
  함께 보고한다. 관련해서 (a) 산출물 파일이 스스로 "원본 + 변경사항 X만 반영한 버전"이라고 선언하고
  있으면(퀴즈생성_프롬프트_템플릿_v4.md 1·4행) 그 파일을 추가 수정할 때 변경사항 섹션도 함께
  갱신해야 선언이 거짓이 되지 않는다. (b) 기록에 "세 번 오갔다" 같은 횟수를 쓸 때는 실제 규칙이
  바뀐 횟수와 논의만 한 횟수를 구분한다 — 인라인 정답 표기는 규칙 변경 2회 + 재확인 1회이지
  변경 3회가 아니다.
- 산출물 문서에 "원본 + 변경사항 X만 반영했다"처럼 **자기 자신의 범위를 선언하는 문장**을 쓰면,
  그 선언 자체가 검증 대상이 된다. 퀴즈생성_프롬프트_템플릿_v4.md에 "이후 반영 사항" 섹션을
  추가해 선언을 고쳤는데도, 검토자가 v3 원본과 문장 단위로 대조해 기록되지 않은 차이 5건(역할
  문장 교체, 문장 4개 삭제, [입력 데이터] 블록 위치 이동, 설계 원리 섹션 재작성)을 더 찾아냈다.
  또 "표기를 통일했다"고 쓴 범위가 실제보다 넓었다(고정 배열·17열 목록만 통일했는데 전부 통일한
  것처럼 읽혔다). 앞으로 (a) 범위를 선언하는 문장은 **원본과 실제로 diff를 떠서 확인한 뒤에만**
  쓰고, (b) "통일했다/전부 반영했다" 같은 표현은 적용 범위를 괄호로 함께 적는다.
- CSV를 근거로 "몇 행"이라고 인용할 때, 셀 안에 줄바꿈이 있으면 **텍스트 파일의 줄 번호와 Excel의
  행 번호가 달라진다**. Story전체 목차_0730.csv는 줄바꿈이 11곳 있어 파일 141줄 = 레코드 130개다.
  대시보드에 "목차 10행"이라고 적었더니 Excel에서는 8행을 가리키는 상태가 됐다. 사람이 열어볼
  산출물에는 행 번호 대신 **값 자체**(분과/중단원/아티클명)를 근거로 적는다.
- 두 문서가 **같은 숫자를 쓰는데 뜻이 달라서** 불일치가 오래 숨어 있었다. SPEC·에이전트 정의는
  자기검수를 "11개 = 원본 10개 + 분과 가이드"로, 템플릿 체크리스트는 "11개 = 원본 10개 + 연도
  암기"로 적어 **서로 다른 항목을 11번이라 불렀다.** 둘 다 "11항목"이라 적혀 있으니 grep으로
  숫자만 맞춰봐서는 어긋난 걸 알 수 없었고, 실제 점검 항목은 12개인데 PRD는 아직 10개였다.
  2026-08-07에 12개로 확정하고 번호(11=연도 암기, 12=분과 가이드)를 SPEC에 못박았다. 앞으로
  체크리스트·목록의 개수를 문서에 적을 때는 **숫자만 쓰지 말고 항목 이름까지 함께 적어**,
  숫자가 같아도 내용이 다른 상태를 잡을 수 있게 한다.
- **브라우저는 로컬 프로그램을 실행할 수 없다.** 대시보드에서 "클릭하면 엑셀로 원본 열기"를 만들려고
  상대경로 링크 → 다운로드(사본 생성) 문제를 겪고, Office의 `ms-excel:ofe|u|` 프로토콜로 바꿨더니
  엑셀만 뜨고 파일은 열리지 않았다. 원인은 레지스트리에 등록된 핸들러가 `protocolhandler.exe`로,
  SharePoint·OneDrive 같은 **웹 URL 전용**이라 로컬 경로를 처리하지 않기 때문이다. 결론: 로컬
  HTML에서 로컬 파일을 앱으로 여는 건 링크로 해결되지 않는다. **전체 경로를 클립보드에 복사**해
  Excel 열기 창·탐색기 주소창에 붙여넣는 방식이 유일하게 확실하다. 앞으로 "클릭 한 번에 로컬 앱
  실행"류 요구가 나오면 먼저 이 제약을 설명하고 대안을 제시한다. 덧붙여, 경로 같은 환경 의존 값은
  파일 곳곳에 박지 말고 `BASE` 상수 한 곳에 모아 폴더 이동에 대비한다.
- **"표시용 라벨"이라고 생각한 곳에서 값을 지어냈다.** 대시보드·미리보기에서 대단원명을
  `1. 빅뱅: 우주의 시작`처럼 **콜론**으로 적었는데, 원본(`재료/Story전체 목차_0730.csv`, 최종 원고
  머리말)은 전부 **엠대시** `1. 빅뱅 — 우주의 시작`이었다. 재료 전체에 콜론 표기는 0건이었다.
  게다가 이 값이 미리보기의 "엑셀 반영 요청서"에 실려 나가도록 되어 있어, 표시용이 아니라 실제
  데이터로 흘러갈 수 있는 상태였다. 또 검수자 이름 입력칸 placeholder에 실명형 이름을 예시로
  지어 넣었다. 교훈: **화면에 보이는 고유명사는 전부 원본에서 복사**하고, 구분자(—, :, ·)까지
  바꾸지 않는다. 예시 placeholder에는 사람 이름 같은 고유명사를 만들어 넣지 않는다.
- **세어보지 않은 숫자를 근거처럼 적었다.** 미리보기의 "검수 정보"에 발문 74자·카드 44~48자·보기
  26~48자라고 써 놓았는데, 검토자가 실제로 세어보니 93자·43~50자·29~60자였다. 서브에이전트가 보고한
  값을 그대로 옮겨 적었고 직접 검증하지 않은 것이 원인이다. 숫자는 "지어낸 값"으로 취급되므로,
  **글자 수·개수·비율처럼 검증 가능한 수치는 반드시 직접 세어 확인한 뒤 적는다.** 다른 에이전트가
  준 수치도 그대로 믿지 말고 한 번은 대조한다.
- **파일이 Excel에 열려 있으면 CSV 쓰기가 중간에 실패해 "절반만 반영된" 위험한 상태가 된다.**
  7행 중 2행만 v2로 바뀌고 나머지가 v1로 남아, 그대로 팀 시트에 붙였다면 문항과 정답이 어긋난 채
  나갈 뻔했다(2026-08-06에도 같은 잠금 사고가 있었다 — 두 번째 재발). 앞으로 대단원별 CSV를 수정하기
  **전에 먼저 쓰기 가능 여부를 확인**하고, 잠겨 있으면 아예 시작하지 않는다. 부분 실패가 발생하면
  즉시 그 사실을 기록에 남기고 사람에게 알린 뒤, 잠금이 풀리면 **전체 행을 다시 써서** 섞인 상태를
  없앤다(실패 지점부터 이어 쓰지 않는다).
- **사람이 직접 풀어보게 하니 자동 검수가 통과시킨 문제들이 무더기로 나왔다.** 자기검수 13항목은
  전부 통과였고 80자 초과 문장도 0건이었는데, 사용자가 문항을 하나씩 풀어보며 지적한 것은 전혀
  다른 층위였다 — 기준계 생략('시간이 느려진다'), 관측 행위와 측정 결과 혼동('본다'), 지시어의
  대상 없음('이 원리'), 가정을 사실로 단정('빛의 매질인 에테르'), 실험 결과 단순화, 개념 종류 오류
  ('기준계'를 '곳'으로), 동음이의어(우주선=spacecraft), 배열 기준이 두 개로 섞임, OX에 거짓이 두 개.
  **형식 체크리스트는 이런 것을 잡지 못한다.** 교훈: (a) 새 유형·새 분과의 첫 문항은 반드시 사람이
  풀어보는 단계를 거친다. (b) 사람이 지적한 내용은 그 문항만 고치고 끝내지 말고 **원리로 승격해
  템플릿에 남긴다** — 2026-08-10에 `★ 문항 작성 원리` 8개 절을 신설한 이유다.
- **파일을 전면 재작성하면서 이 LESSONS에 이미 적혀 있는 실수를 세 건 다시 저질렀다.** 2026-08-11에
  퀴즈 미리보기를 처음부터 다시 만들었는데, 검토자가 (a) 문항 6의 "보기 길이 **26~48자**" — 2026-08-07에
  이미 오류로 판정돼 29~60자로 정정된 값을 낡은 파일의 raw 문구에서 그대로 복사, (b) 사람 확인 지점
  블록이 footer **앞**에 놓여 "결과물 맨 아래" 규칙 위반, (c) CLAUDE.md 「미발행 원고 외부 저장 금지」
  확인 항목 누락 — 세 건을 모두 다시 찾아냈다. 셋 다 이 파일에 이미 항목이 있다. 원인은 **기존 파일의
  문구를 복사하면 그 안의 낡은 수치와 잘못된 구조까지 함께 복사되는데** LESSONS를 대조하지 않은 것이다.
  앞으로 산출물을 새로 만들거나 전면 재작성할 때는 (1) LESSONS를 체크리스트로 한 번 훑고, (2) 복사해
  온 수치는 LESSONS·템플릿에 정정 기록이 있는지 grep으로 확인한 뒤에 넣는다.
- **화면 표시를 렌더 시점 값으로만 계산하면 그 뒤의 상태 변경과 어긋난다.** 검수 메모 토글의 「작성됨」
  배지를 `render()` 때 localStorage 값으로만 계산해 두었더니, 메모를 초기화해도 배지가 남고 새로 써도
  배지가 붙지 않았다. 같은 종류로, 「선택 초기화」에서 `lsDel(키)`를 부른 직후 `render()→updateBar()`가
  같은 키를 다시 써서 삭제가 무효가 됐다. 교훈: **저장·삭제와 표시 갱신을 같은 경로에 묶는다.** 단
  전체 재렌더는 사용자가 풀고 있던 답을 날리므로, 국소 갱신 함수(`syncMemoBadge()` 같은)를 따로 둔다.
- **같은 파일 안에서 글자 수 계수 기준이 갈렸다.** "발문 93자 → 19자"라고 적었는데 93은 쉼표·물음표를
  포함해 센 값이고 19는 물음표를 뺀 값이었다(포함하면 20자). 다른 수치들은 모두 문장부호 포함 기준이었다.
  교훈: 글자 수를 적을 때는 **계수 기준을 함께 적고**, 한 파일 안에서 기준을 섞지 않는다.
- **"왜 이 기능이 없냐"는 물음은 기능 누락이 아니라 도입 시점 차이일 수 있다.** 사용자가 특수상대성이론
  미리보기에 A/B가 없는 이유를 물었을 때, HISTORY 시간순을 보니 A/B 2안 방식이 그 아티클 처리 **이후에**
  신설된 것이 원인이었다. 코드를 뜯어보기 전에 기록을 먼저 확인하면 답이 바로 나온다. 더불어 **새 방식을
  도입할 때 이미 처리된 산출물에 소급 적용할지를 그 자리에서 정해 두면** 이런 혼란이 생기지 않는다.
- **폐기한 규칙은 "폐기했다"는 설명조차 프롬프트 본문에 남기면 안 된다.** 2026-08-12에 60자/80자
  글자 수 상한을 없앴는데, 그 경위를 설명하는 문장을 프롬프트 본문 안에 적어둔 탓에 "60자 안팎·80자
  초과 시 자동 미통과"라는 **지우려던 숫자가 프롬프트에 계속 노출**되고 있었다. 이력과 실행용 본문이
  한 파일에 섞여 있어 생긴 문제로, 같은 자리에서 (a) 자기검수 13번의 "아래 주 참고"가 가리킬 주가
  코드블록 안에 없는 **끊어진 참조**였고, (b) v4 변경이력의 "설계 원리 불릿 2개→4개"가 실제
  (v3 4개 → v4 6개)와 다른 **오기**였다 — 세어보지 않고 적은 숫자가 또 나온 것이다. 2026-08-14에
  v5를 만들며 실행용 프롬프트(`퀴즈생성_프롬프트_템플릿_v5.md`)와 이력(`프롬프트_변경이력.md`)을
  파일로 분리했다. 앞으로 (1) 실행용 프롬프트에는 날짜·버전 태그·변경 경위를 넣지 않고, (2) 규칙을
  폐기할 때는 **그 규칙의 숫자가 본문에 한 번도 남지 않는지 grep으로 확인**한다.
- **`재료/`에는 기준 문서(CLAUDE.md·PRD·SPEC·PLAN)의 낡은 사본이 있다.** 자기검수 항목 수를 확인하려고
  `재료/SPEC_기능명세서.md`를 읽었더니 "11개 체크 항목"이라 적혀 있어 문서 간 불일치가 남아 있는 줄
  알았는데, **루트의 `SPEC_기능명세서.md`는 이미 14개로 갱신돼 있었다**(재료 사본은 PRD도 10항목,
  CLAUDE.md도 11항목인 옛 상태). `재료/`는 수정 금지 폴더라 사본이 갱신되지 않은 채 남아 있는 것이다.
  앞으로 기준 문서를 근거로 인용할 때는 **루트의 파일을 읽고**, `재료/` 경로에서 읽었다면 루트 사본과
  대조한 뒤에 말한다.
- **중단된 서브에이전트를 재개할 때 대상(원고)을 다시 명시하지 않으면 스스로 다른 대상을 고른다.**
  2026-08-14 세션 한도로 퀴즈생성자 4건이 중단된 뒤 "이어서 진행해 주세요"만 보내 재개했더니, 아직
  결과를 내지 않았던 2건이 **원고를 스스로 다시 골라** 둘 다 `1_2)_A표준 우주 모형`을 처리했다.
  원래 맡았던 `3_1)_A_원자`·`3_2)_A_원소의주기적성질`은 처리되지 않았고, 표준 우주 모형만 서로 다른
  내용으로 **2벌** 생겨 사람이 하나를 골라 버려야 하는 낭비가 났다(한 건은 "큐의 다음 건"이라고
  스스로 판단했다고 밝혔다). 앞으로 재개 메시지에는 **대상 파일의 절대경로를 반드시 다시 적고,
  "원고를 스스로 다시 고르지 말 것"을 명시**한다. 신규 호출도 마찬가지다.
- **서브에이전트의 결과가 길면 앞부분이 잘린 채 도착할 수 있다.** 같은 날 `3_1)_A_원자` 건은 정상
  완료로 보고됐지만 실제로 전달된 것은 **뒷부분(B안 자기검수 후반 + 검수 요청)뿐**이었고 14문항
  본문이 통째로 없었다. 완료 보고만 믿고 넘어갔다면 문항 없이 반영 단계로 갈 뻔했다. 앞으로 퀴즈생성자
  결과를 받으면 **1~5단계(메타데이터·축 설계·14문항·자기검수 2건·검수 요청)가 다 있는지 먼저 확인**하고,
  빠졌으면 빠진 부분만 지정해 다시 요청한다(전체 재작성은 토큰 낭비).
- **선택 상태를 바꿀 때마다 전체를 재렌더하면 사용자가 풀던 답이 지워진다.** 미리보기 v3에서 A/B
  선택을 체크박스로 바꾸며 선택이 잦아졌는데, v2 방식대로 `render()`를 부르면 매번 답이 초기화된다.
  선택 상태만 반영하는 `applySelUI()`를 따로 두어 해결했다. 다만 이때 **재렌더에 얹혀 있던 부수 갱신이
  같이 멈추는 것**을 놓치기 쉽다 — 실제로 검수 메모 기본 양식의 머리글("문항 3 · 중 · … (미선택)")이
  갱신되지 않아 A안을 고른 뒤 메모를 써도 "(미선택)"이 남았고, 「7개 전부 선택」 버튼은 탭 모드에서만
  재렌더가 돌아 **같은 버튼이 보기 모드에 따라 다르게 동작**했다. 재렌더를 걷어낼 때는 **그 안에서
  같이 돌던 갱신이 무엇이었는지 먼저 세어보고** 각각 대체 경로를 만든다.
- **화면에 붙여 놓은 코드는 개수가 잘못 읽힌다.** 선택 현황을 `1AB`처럼 이어 붙여 표시했더니 사용자가
  "두 개가 아니라 한 개를 고른 것 같다"고 지적했다. A와 B를 각각 네모 테두리로 감싸 해결했다. 앞으로
  **여러 항목을 한 칸에 표시할 때는 항목마다 테두리·간격으로 경계를 준다** — 문자열 연결로만 두지 않는다.
- **서브에이전트 응답이 길면 앞부분부터 잘려 전달된다 — 가장 중요한 것을 맨 앞에 둬야 한다.**
  2026-08-14~15에 `퀴즈생성자` 결과가 **6번 잘렸다**(원자 / 비금속 / 지구시스템 / 우주론 / 양자물리 /
  대기의순환). 매번 뒷부분(자기검수·기획자 검수 요청)만 도착하고 **14문항 본문이 통째로 사라져** 다시
  요청해야 했다. 원인은 출력 순서였다 — 에이전트 정의의 「출력 형식」이 메타데이터 → 축 설계 → 문항 →
  자기검수 순이라, 앞의 설명이 길어질수록 정작 필요한 문항이 뒤로 밀려 잘려 나갔다.
  더 나쁜 것은 **완료 상태로 보고된다**는 점이다. 잘린 줄 모르고 넘어가면 문항 없이 반영 단계로 간다.
  2026-08-15에 두 가지를 고쳤다. (1) `퀴즈생성자.md`의 출력 순서를 **문항 17열 데이터 → 메타데이터 →
  축 설계 → 자기검수 → 검수 요청**으로 뒤집고, 자기검수는 한 줄 목록으로, 축 설계는 표 하나로 압축하고,
  TSV 블록은 만들지 않도록 명시했다. (2) 결과를 받으면 **1~5단계가 다 있는지 먼저 세어 보고**, 빠졌으면
  **빠진 부분만 지정해** 다시 요청한다(전체 재작성은 토큰 낭비).
- **생성기의 템플릿이 곧 생성 결과물이면, 그 파일을 다시 생성하는 순간 도구가 망가진다.**
  `미리보기생성.js`가 `퀴즈미리보기/01_빅뱅_A_특수상대성이론_v3.html`을 템플릿으로 읽으면서 동시에
  그 파일을 출력으로도 썼다. 카드 일괄 수정 때 그 파일을 먼저 재생성하자 데이터 블록이 새 형식(작은따옴표
  → 큰따옴표)으로 덮여 교체 표시를 못 찾게 됐고, **이어진 12건이 연쇄로 전부 실패**했다. 자기 자신을
  덮어쓸 수 없도록 템플릿을 `산출물/도구/미리보기_템플릿.html`로 분리하고, 표시 찾기를 정규식으로 바꿔
  따옴표 종류에 흔들리지 않게 했다. **템플릿·설정처럼 "읽기 전용이어야 하는 입력"은 출력 폴더 밖에 둔다.**
- **규칙을 프롬프트에만 적으면 매번 다시 적어야 하고, 언젠가 빠뜨린다.**
  2026-08-15에 확정한 규칙 3가지(A/B 대원칙 2 변경 · 직접 입력 정답 공백 무시 · 순서배열 카드 단독
  가독성)를 처음에는 호출 프롬프트에만 넣고 돌렸다. 사용자가 "매번 적지 않아도 되게 하라"고 지적해
  `.claude/agents/퀴즈생성자.md`·`검토자.md`, 템플릿 v4·v5, `퀴즈_AB_2안_생성기준.md`,
  `SPEC_기능명세서.md` 자기검수 7·8번, `CLAUDE.md`에 모두 반영했다. **규칙이 바뀌면 그 규칙을 읽는
  주체(에이전트 정의·템플릿·검수 기준·SPEC)를 전부 찾아 함께 고친다.** 프롬프트는 그때그때 다른 것
  (원고 경로·소재 겹침 정보)만 담는다.
- **"최우선 절"이 있는 문서는 규칙을 바꿀 때 그 절부터 확인한다.** 2026-08-28에 직접 입력 형식을
  바꾸면서 v5의 「5번 — 직접 입력」 절과 자기검수 항목은 고쳤는데, **`★ 문항 작성 원리` 원리 6에 남은
  옛 문장("예시는 [예문] 칸에, 묻는 한 문장만 [문제] 칸에 둡니다")을 놓쳤다.** 하필 이 절은 v5 자신과
  `퀴즈생성자.md`가 **"충돌하면 이 절을 기준으로 판단한다"**고 선언한 절이라, 그대로 뒀으면 **폐기한
  옛 규칙이 새 규칙을 이기는** 상태가 될 뻔했다(검토자가 발견). 앞으로 규칙을 바꿀 때는 ① 유형별 절
  ② 공통 원칙 ③ **상위 요약·최우선 절** ④ 자기검수 항목 네 곳을 세트로 확인한다. 상위 요약 절은
  본문을 요약한 것이므로 **본문만 고치면 반드시 어긋난다.**
- **"어디에 반영했다"고 적기 전에 그 파일을 열어 확인한다.** 같은 날 SPEC과 진행상황에 A/B 키워드
  차별화를 **"자기검수 4번에 반영"**이라고 적었는데, 정작 4번 원문이 있는 v5에는 한 글자도 넣지
  않았다. 부속 문서(`퀴즈_AB_2안_생성기준.md`)에만 넣고 반영했다고 기록한 것이다. 검토자 세 명 중
  둘이 같은 지점을 잡았다. **반영 위치를 문서에 명시할 때는 그 위치를 grep으로 확인하고 적는다.**
- **예시를 들 때 그 예시가 원고에 실재하는지 확인한다.** 비유 금지 규칙을 쓰면서 `결합 손`의 대체
  용어로 "원고에 함께 나오는 정식 용어(`공유 결합`·`원자가 전자`)"라고 적었는데, **`원자가 전자`는
  `결합 손`이 나오는 두 원고 어디에도 없었다**(검토자가 원고를 열어 확인). "지어내지 말라"는 규칙을
  쓰면서 그 문장 안에서 지어낸 셈이다. 대체어 목록을 없애고 "같은 원고 안에서 그 개념을 가리키는
  정식 용어를 찾아 쓰라"는 지시로 바꿨다. **규칙 문서의 예시도 재료 대조 대상이다.**
- **사용자 지침을 옮길 때 없던 우선순위·조건·수치를 붙이지 않는다.** 사용자는 A/B를 "서로 다른 핵심
  키워드**나** 학습 포인트로" 구성하라고 **병렬**로 말했는데, 문서에는 "1순위는 핵심 키워드", "키워드가
  둘 이상이면 → 키워드로, 하나뿐이면 → 학습 포인트로"라는 **순위와 조건 분기**가 붙었다. "어느 쪽을
  골라도 학습 가치가 같아야 한다", "넷이나 나오는 경우는 드물다" 같은 근거 없는 요건·빈도 주장도
  들어갔다. 2026-08-12에 글자 수 "60자/80자"를 임의로 붙였다가 폐기한 것과 **같은 종류의 실수**다.
  옮겨 적을 때는 **사용자 문장의 접속사(나/또는 ↔ 먼저/그다음)와 강도(가능한 한 ↔ 반드시)를 그대로
  유지**하고, 판단 기준을 새로 만들지 않는다.
- **"문서에 규칙이 있다"와 "구현이 그 규칙을 지킨다"는 다른 문제다.** 2026-08-15에 "직접 입력 채점은
  공백을 무시한다"를 확정하고 SPEC·템플릿·에이전트 정의에 전부 반영했는데, **미리보기의 채점 코드는
  입력값만 공백을 지우고 정답은 그대로 비교하고 있었다.** 그래서 `정역학적 평형`처럼 띄어 쓴 정답이
  13일 동안 전부 오답 처리되고 있었고, 사용자가 같은 규칙을 다시 알려줄 때까지 아무도 몰랐다.
  규칙이 **채점·표시 같은 동작에 관한 것이면 문서만 고치지 말고 그 동작을 실제로 한 번 돌려서 확인**한다
  (정답을 띄어 써 넣어 보기 / 예문 없는 문항 화면 보기 수준으로 충분했다).
- **데이터가 `-`로 자리를 지키는 규칙과, 그것을 화면에 보여주는 것은 별개다.** 예문 없는 칸에 `-`를
  넣는 규칙(`X` 금지 — OX 정답과 혼동)은 **17열 데이터의 규칙**인데, 템플릿이 `if (item.ex)`로만 검사해
  `-` 한 글자가 든 빈 지문 박스를 그리고 있었다. **"빈 값을 표현하는 문자"는 저장용이고, 표시할 때는
  빈 값으로 취급**해야 한다. 앞으로 `-`·`N/A` 같은 자리 지킴 문자를 도입하면 표시 코드도 함께 본다.
- **표기 기호를 정할 때 그 프로젝트에서 이미 쓰는 기호와 겹치는지 본다.** 순서배열 카드 라벨로
  A·B·C·D가 자연스러워 보였지만, 이 과제는 **A안·B안**으로 14문항을 관리해서 "4A의 정답은 B-D-A-C"처럼
  안(案) 기호와 카드 라벨이 한 문장에 섞인다. `가`·`나`·`다`·`라`로 정해 충돌을 없앴다. 기호를 새로
  도입할 때는 **기존 기호 목록과 대조**한다.
- **표시 형식이 바뀌어도 데이터 구조는 안 바꾸는 쪽이 싸다.** 카드 라벨을 숫자에서 `가`·`나`·`다`·`라`로
  바꿀 때 JSON의 `order.ans`를 라벨 문자열로 옮길 수도 있었지만, **숫자 배열로 두고 화면에서 변환**하게
  했다. 덕분에 이미 만든 5건을 다시 만들지 않았고 검증 스크립트도 그대로 쓸 수 있었다. 바꿔야 하는 것이
  **표시**인지 **저장**인지 먼저 가른다.
- **사람이 손으로 옮겨 적는 단계가 파이프라인에 있으면 검사 도구를 붙인다.** `퀴즈생성자`는 Write 권한이
  없어 결과를 사람이 JSON으로 옮겨 담는데, **정답 번호 하나만 틀려도 미리보기가 틀린 답을 정답으로
  표시**한다. `산출물/도구/퀴즈JSON검증.js`를 만들어 저장 직후 돌리게 했다. 다만 이 검사는 **구조와 자기
  일관성만** 본다 — 정답이 원고에 맞는지는 여전히 사람이 미리보기에서 확인해야 한다.
- **한국어 낱말을 일괄 치환할 때는 조사와 단위 명사를 함께 본다.** '카드'(모음 끝) → '문장'(자음 끝)으로
  94군데를 바꾸자 `문장는`·`문장를`·`문장가`·`문장로`가 68군데 생겼고, `카드 문장의 용어를` → `문장 문장의
  용어를` 같은 **중복 낱말**과 `카드 4장`(→ `문장 4개`) 같은 **단위 명사** 문제도 나왔다. 게다가 마크다운
  강조가 조사 사이에 끼어(`문장**가`) 단순 문자열 치환으로는 잡히지 않는 것이 2군데 있었다.
  절차: ① 어색해지는 문구를 **먼저 문구째로** 바꾼다 ② 단위 명사를 정규식으로 바꾼다 ③ 낱말을 치환한다
  ④ **조사 교정**(는→은, 를→을, 가→이, 로→으로, 와→과)을 강조·코드 표시를 허용하는 정규식으로 돌린다
  ⑤ 바뀐 문단을 **눈으로 한 번 읽는다.** ⑤를 건너뛰면 문법이 깨진 프롬프트를 에이전트에게 주게 된다.
- **학습자가 읽는 말과 구현상의 이름을 구분한다.** 순서배열 항목을 코드에서는 `ocard`, 문서에서는 '카드'로
  불렀는데 그 말이 **발문에까지 흘러 들어가** 학습자 화면에 노출됐다. 사용자가 '문장'으로 바로잡았다.
  UI 구현 용어를 규칙 문서에 쓰면 발문에 그대로 새어 나가므로, **발문에 쓸 말을 규칙에 명시**하고
  구현 용어는 코드 안에만 둔다. 발문 표기만 고치면 규칙 본문을 보고 되돌아가므로 **어휘를 한쪽으로 모은다.**
- **"고쳤다"는 기록이 그 코드가 파일에 있다는 증거는 아니다.** HISTORY에는 미리보기 템플릿에
  `ORD_LABEL`·`ordLabel()`·`ordAnsText()`를 넣었다고 적혀 있었지만 **실제 파일에는 호출부만 있고 정의가
  없었다.** 그 상태로 미리보기 5건이 생성되어, 순서배열 문항에서 ReferenceError로 화면이 아예 그려지지
  않았다. 규칙 문서(v5·SPEC·CLAUDE.md)는 전부 맞게 고쳐져 있었기 때문에 **문서만 대조하면 통과로 보인다.**
  절차: 규칙을 바꿨으면 ① 문서 ② **그 규칙을 실행하는 코드** ③ **이미 만들어 둔 산출물** 세 곳을 각각
  확인하고, 기록에 "넣었다"고 쓰기 전에 `grep`으로 정의가 있는지 본다.
- **검사 도구가 보지 않는 층이 어디인지 알고 있어야 한다.** `퀴즈JSON검증.js`는 JSON 구조만 보므로 화면
  전용 함수가 통째로 사라져도 **"전항목 통과"가 나온다.** 반대로 화면이 라벨로 바꿔 보여주는 값(검수
  정보의 `정답 1`)은 JSON 문자열이라 검증기가 볼 수 있는데도 검사 항목이 없어 20건이 옛 숫자 표기로
  남아 있었다. 표시 규칙을 바꿀 때는 **그 규칙을 지키는지 확인해 줄 도구가 있는지**를 함께 정하고,
  없으면 그 자리에서 만든다 — 이번에는 생성기에 "템플릿에 함수 정의가 없으면 중단"을, 검증기에
  "`정답 1`이 `ans`의 라벨 형식과 같은가"를 넣었다.
- **가드를 넣었으면 일부러 깨뜨려 걸리는지 본다.** 새로 넣은 검사 2개를 각각 고장 낸 사본으로 돌려
  실제로 중단·불일치가 나는 것을 확인한 뒤 원상복구했다(md5로 복구 확인). 통과만 보고 끝내면 **검사가
  실제로는 아무것도 안 하는 상태**를 알아채지 못한다.
- **표기 규칙을 바꿀 때는 "표기"와 "채점"을 분리해서 말한다.** 빈칸 표기를 `_`로 바꾸며 띄어쓰기를
  살렸지만 **채점은 여전히 공백을 무시**한다. 두 가지를 한 문장에 뭉쳐 쓰면 "띄어쓰기를 반영한다"가
  채점까지 엄격해진다는 뜻으로 읽힌다. 규칙 문서 6곳에 **"표기가 띄어쓰기를 보여주는 것과 채점이 공백을
  무시하는 것은 별개"**를 같이 적었다.
- **표기 문자를 바꾸면 그 문자를 잡던 정규식·CSS도 같이 본다.** `○`는 한 덩어리(`/○+/g`)로 잡히지만
  밑줄 표기는 **사이에 공백이 들어가서** 같은 방식으로는 `__ ____ __`가 세 조각으로 쪼개진다.
  `/_+(?: _+)*/g`로 바꾸고, 줄바꿈으로 끊기지 않게 `white-space:nowrap`도 함께 넣었다. 표기를 바꿀 때는
  ① 규칙 문서 ② 그 표기를 만드는 쪽 ③ **그 표기를 읽는 쪽(정규식·검증기·CSS)** 을 한 묶음으로 고친다.
- **현황판을 갱신할 때 표만 고치고 산문 문장을 놓친다.** 2026-09-01 별 반영 후 `진행상황.md`의 표
  수치(4건 → 5건, 대기 1건 → 0건)는 고쳤는데, 같은 파일의 산문 서술 세 곳이 어제 기준으로 남았다 —
  0-1장 "사람 선택 대기 상태다", 5장 "CSV 반영은 24건 전부 대기 상태다 — 빈 스켈레톤이다", 그리고
  "2026-08-31에 4개 아티클이 채워졌다". **표와 본문이 정면으로 충돌하는 파일**이 되어, 다음 세션이
  어느 쪽을 믿을지 알 수 없다. 절차: 숫자를 고쳤으면 그 숫자가 **문장으로도 적혀 있는 자리**를
  `grep`으로 찾아 함께 고친다(예: `grep -n "대기\|스켈레톤\|4개\|4건"`). 표만 고치는 것은 갱신이 아니다.
- **간섭 쌍은 "앞 건에 적었으면 이번 건에도 적는다".** 앞 4건은 「동시 선택 시 정답이 노출되는 쌍」을
  기록했는데 별 1건만 빠졌다. 실제로 별 확정 선택에는 `3A+7B`(7B 보기4 = 3A 정답)와 `1B+6A`(1B 예문이
  6A 정답 보기 2개를 알려줌) 두 쌍이 들어 있었다. 규칙 위반은 아니고 사람이 고른 것이라 반영은 그대로
  하지만, **적지 않으면 다음에 같은 조합을 권할 때 근거가 사라진다.** 반영 직후 선택 7개를 서로
  대조해 간섭 여부를 한 번 훑고, 없으면 "없음"이라고 적는다.
- **세는 값은 세어 보고 적는다.** 서브에이전트가 "예문 없는 6문항은 `-`를 유지했다"고 기록했으나 실제로는
  5문항이었다(1번 인라인과 5번 직접 입력에는 예문이 있다). 7에서 2를 빼는 자명한 계산이라 검토 없이는
  넘어간다. **개수를 적을 때는 어느 항목인지 함께 적는다** — "5문항(2·3·4·6·7번)"처럼 쓰면 세지 않고는
  쓸 수 없어 틀린 수가 남지 않는다.
- **"권장"을 문서에 넣을 때는 "필수"와 섞이지 않게 층을 분리한다.** 2026-09-01에 모범 문항 지향
  3가지를 넣으면서 처음에는 자기검수 18~20번을 `(필수)`로 초안했는데, 사용자가 **"이런 문제 아니면
  만들지마가 아니라, 되도록 이렇게 만들어줘. 이렇게 못 만드는 문제는 기존 출제대로 만들면 됨"**이라고
  바로잡았다(원문 그대로). 필수로 넣었다면 원고 조건상 그
  형태를 못 만드는 문항이 전부 **미통과**가 되어 생성이 막히거나, 통과시키려고 소재를 비트는 —
  규칙 3번(지어내지 않는다) 위반이 유도됐을 것이다. 방법: ① 마크를 다르게 준다(`★` 필수 / `☆` 지향)
  ② 판정 어휘를 다르게 준다(`통과`/`미통과` vs `달성`/`해당 없음`/`미달성 — 사유`) ③ 항목 수를 늘리지
  않는다(필수는 17 그대로, 지향은 별도 블록). 같은 목록에 섞어 번호만 이어 붙이면 다음 세션이 전부
  필수로 읽는다.
- **권장을 추가하면 "검사하는 쪽"에도 권장임을 알려야 한다.** `검토자.md`에 **"지향 항목 미달성을
  「규칙 위반」으로 보고하지 않는다"**를 명시하지 않으면, 검토자는 문서에 적힌 항목을 성실히 위반으로
  잡아 사용자 지시와 정반대로 작동한다. 규칙을 바꿀 때 고칠 세 곳(문서 / 실행하는 코드 / 이미 만든
  산출물)에 더해 **판정하는 에이전트**가 넷째 자리다.
- **근거가 갈린 지점을 찾을 때는 "같은 조건에서 갈린 쌍"을 먼저 찾는다.** 모범 문항 8건의 공통점만
  나열했으면 "인라인이 좋다" 같은 쓸모없는 결론이 나왔을 것이다. 우주론이 **1A·1B를 둘 다 고르고
  1B에만 별표**를 준 것을 찾아내자 아티클·검수자·유형·난이도가 전부 통제되어 차이 하나만 남았다.
  분석할 때는 별표된 것만 보지 말고 **같은 자리에서 별표 안 된 대조군**을 반드시 함께 뽑는다.
- **인용은 옮겨 적지 말고 원문을 복사한다.** 2026-09-01에 사용자 발언 한 문장을 문서 6곳에 적으면서
  **여섯 곳이 전부 조금씩 달라졌다** — "만들어 줘" / "만들어 달라" / 두 문장을 한 문장으로 합침 /
  둘째 문장 통째로 누락. 문항 인용 4건도 첫 어절(`지구의`·`태양 중심에서`)이나 마침표가 빠졌다.
  손으로 옮기면 매번 조금씩 다듬게 되고, 나중에 어느 판이 원문인지 알 수 없어진다.
  절차: ① 원문을 한 번만 변수/스크립트에 담아 **같은 문자열을 여러 파일에 넣는다** ② 인용 뒤에
  "(원문 그대로)"를 붙여 다듬으면 안 되는 자리임을 표시한다 ③ 문항을 인용할 때는 JSON에서 복사하고
  줄이지 않는다. `LESSONS`에 이미 「사용자 문장의 접속사와 강도를 그대로 유지한다」가 있었는데도
  같은 실수를 했다 — **규칙을 아는 것과 옮겨 적는 습관을 바꾸는 것은 다르다.**
- **예외를 쓸 때 "만"을 쓰면 범위가 잘린다.** 지향 항목을 위반 판정에서 빼려고 `검토자.md`에
  "위 여덟 규칙과 필수 17항목**만이** 위반 판정 대상이다"라고 썼는데, 이 한 단어가 같은 문서 앞부분의
  「③ 규칙 위반」 정의(CLAUDE.md 규칙·SPEC 표기 규칙·**CSV의 BOM 누락**)까지 판정에서 제외해 버렸다.
  예외를 추가할 때는 **"A만 대상이다"가 아니라 "종전 범위 그대로이고 B만 빠진다"**로 쓴다.
  빼는 것만 명시하고 남는 범위는 건드리지 않는다.
- **"반영했다"고 적은 목록은 검토로 확인한 뒤에 적는다.** 0-3장에 "반영한 문서 7종"이라 썼는데,
  검토 결과 실제로 손대야 했던 곳은 **11종 + 도구 1종**이었다. 빠졌던 자리는 전부 "항목 수·리포트
  구성·잘림 확인 목록을 설명하는 대목" — **새 항목을 추가하면 그 항목을 세거나 나열하는 모든 자리가
  같이 바뀐다.** 찾는 법: `grep -rn "17항목\|자기검수\|2세트\|잘림"` 처럼 **개수와 목록 어휘로** 훑는다.
- **클라이언트 렌더링 산출물은 정적 grep으로 검증할 수 없다.** 미리보기 HTML에서 `class="ex"` 개수를
  세어 "예문 있는 문항 4개인데 지문 박스가 2개뿐"이라며 5개 파일 전부를 위반으로 판정했는데,
  **박스는 브라우저에서 JS가 그리므로 파일 안의 두 건은 렌더 함수 소스**였다. 실제 구현은 정상이었다.
  절차: 산출물이 **런타임에 만들어지는 것인지 파일에 박혀 있는 것인지** 먼저 구분하고, 런타임 생성물은
  **생성 로직(판정식) 자체를 읽어** 검증한다. 개수를 세는 검증은 파일에 박힌 값에만 쓴다.
- **"모두 반영되어 있다" 같은 총괄 문장은 시간이 지나면 거짓이 된다.** `CLAUDE.md`가 "아홉 규칙은
  에이전트 정의에 모두 반영되어 있다"고 단언했지만 실제로는 여덟 개였고, 9번은 성격상(화면 표시 규칙)
  애초에 넣을 자리가 없었다. 총괄 문장을 쓸 때는 **예외를 그 자리에 함께 적는다** — "1~8번은 …,
  9번은 …가 구현하고 …가 확인한다"처럼. 그리고 일부러 뺀 항목에는 **"여기에 추가하지 마라"**를
  대상 문서에 적어 둔다. 그러지 않으면 다음 세션이 "빠졌다"고 보고 채워 넣는다.
- **확정·CSV 반영이 끝난 뒤에도 원고는 바뀐다 — 문항은 "완료"가 아니라 "그 판 원고에 대한 완료"다.**
  2026-09-01에 팀 시트 비고에서 `#9 B. 우주론 — 260831 : 본문 일부 수정`을 발견했다. 우주론은 기획자
  검수·모범 문제 지정·CSV 반영까지 끝난 아티클이었는데, 우리가 쓴 원고는 **8/14 판**이었다. 개정본과
  diff하니 w 문단이 전면 재작성되어 **오답 보기 하나가 원고에 없는 서술**이 되어 있었다.
  절차: ① 팀 시트의 **비고(변경 이력) 열을 원고 파일 날짜와 대조**해 미반영 수정을 골라낸다 —
  날짜만 비교하면 되고 비용이 거의 없다 ② 원고가 바뀌면 **그 아티클의 확정 문항 전체를 재판정**한다
  (이번엔 7문항 중 무영향 4·경미 1·검토 1·수정 1이었다) ③ **전체 재생성으로 도망가지 않는다** —
  설계 의도가 살아 있으면 바뀐 칸만 고치는 것이 맞다. 여기서는 `퀴즈생성자` 24분 + 기획자 재검수를
  아꼈고, 멀쩡한 6문항과 모범 문제 2건을 그대로 지켰다.
- **한 칸만 고칠 때는 "한 줄만 바뀌었는가"를 기계로 확인한다.** CSV 한 칸을 문자열 치환으로 고치기 전에
  **그 문자열이 파일 안에서 유일한지 세고**(1건 확인), 고친 뒤 **줄 단위 diff로 달라진 줄이 정확히
  하나인지** 확인했다(파일 27행). 열 수·BOM·줄 수도 함께 봤다. 눈으로 보면 통과처럼 보이는 수정에서
  다른 줄이 함께 망가지는 사고가 이 프로젝트에 이미 2회 있었다.
- **CSV를 고치면 그 근거 JSON도 같이 고친다.** 미리보기는 JSON에서 생성되므로, CSV만 고치면 **화면과
  시트가 서로 다른 답을 보여주는 상태**가 된다. 이번에는 `slots[2].A.single.opts[3]`과 `raw`(갱신 경위)를
  함께 고치고 미리보기를 재생성한 뒤 JSON↔CSV 전 칸 대조로 일치를 확인했다.
- **팀이 준 파일의 인코딩을 먼저 확인한다.** 새 목차 CSV는 **cp949**(Excel 기본 저장)였고 기존 목차는
  UTF-8이었다. `퀴즈생성자`는 Read/Glob/Grep만 있어 cp949를 열면 한글이 깨진 채로 매칭을 시도한다.
  `.xlsx`도 마찬가지로 바이너리라 열 수 없다. 팀 파일을 받으면 **인코딩·확장자를 먼저 보고, 필요하면
  UTF-8 변환본을 만든 뒤에** 에이전트에게 넘긴다.

- **자동 생성물은 "안 바뀐 것"과 "못 만든 것"이 화면에서 똑같아 보인다.** 대시보드가 8/28 이후 12일간
  낡은 수치(검수 완료 0건)를 보여주고 있었는데, 원인은 `산출물/대단원별/`에 대단원이 아닌
  `퀴즈샘플_260831.csv`를 하나 놓은 것이었다. 생성기는 "대단원 CSV 10개 vs 목차 9개"로 판단해
  예외를 던지고 멈췄지만, **아무도 그 콘솔 메시지를 보지 않았고 HTML은 옛 내용 그대로 남아 있었다.**
  → 집계 폴더에는 집계 대상만 둔다. 도구가 폴더를 통째로 읽는다면 **대상 파일을 이름 규칙으로
  좁히고(예: 두 자리 번호로 시작), 규칙 밖 파일은 멈추지 말고 제외하고 알린다.** 그리고 자동 생성
  파일을 근거로 보고하기 전에 **생성기를 한 번 돌려 경고 없이 끝나는지** 확인한다.
- **검사 코드가 특정 파일 하나를 이름으로 붙들고 있으면, 그 파일이 사라진 날부터 검사가 조용히 죽는다.**
  `checkPreviewSync`는 `01_빅뱅_A_특수상대성이론.html`만 대조하도록 박혀 있었는데, 그 파일이 이관 때
  빠지면서 매번 "미리보기가 없어 건너뜁니다" 경고만 내고 아무것도 검사하지 않았다. 경고가 뜨고
  있었는데도 정상 출력의 일부처럼 보여 아무도 손대지 않았다.
  → **검사 대상은 이름으로 박지 말고 폴더를 훑어 정한다.** 그리고 "건너뛴다"는 메시지가 매번 나오면
  그건 통과가 아니라 **검사가 없는 상태**임을 뜻한다.
- **A안·B안의 발문이 같은 문항 유형에서는 "문제 텍스트로 후보 찾기"가 엉뚱한 안을 집는다.**
  직접 입력(5번)은 [문제] 칸이 `빈칸에 들어갈 알맞은 말을 쓰세요.`로 고정이라 A·B가 같다.
  새 대조 로직이 처음에 `find(문제 일치)`로 첫 후보만 집는 바람에, 기획자가 B안을 고른 2건
  (`B. 우주론` 6번 · `C. 생명과 물` 5번)이 "미리보기와 다름"으로 **거짓 경고**가 났다.
  → 후보를 짝지을 때는 **한 칸만 보지 말고, 문제가 같은 후보를 모두 모아 예문·정답해설까지 맞는 것이
  하나라도 있으면 통과**로 본다. 고정 발문 규칙(CLAUDE.md 문항 규칙 6번)이 있는 유형은 특히 그렇다.
- **비교 대상에 "화면 전용으로 채워 넣는 값"이 섞이면 멀쩡한 것도 다르다고 나온다.**
  미리보기 HTML과 JSON을 `JSON.stringify`로 통째 비교하려 했는데, `미리보기생성.js`가 `raw`가 비어
  있으면 HTML 쪽에만 채워 넣는다. 그대로 두면 **방금 만든 미리보기도 "낡았다"고 경고**할 뻔했다
  (지금 6건은 `raw`가 다 들어 있어 드러나지 않던 구멍이었다).
  → 두 산출물을 대조할 때는 **생성 과정에서 파생되는 값을 빼고** 비교한다.
- **"확인했습니다"라고 쓴 문장이 실제로 확인한 범위와 다를 수 있다.** 대시보드 푸터는 "모든 파일이
  17열 구조임을 확인했습니다"라고 썼지만 검사는 **각 파일의 헤더 한 줄**만 봤다. 데이터 행이 16열이
  되어도 같은 문장이 나온다. → 산출물에 적는 확인 문구는 **검사 코드가 실제로 센 범위를 그대로**
  쓴다(지금은 "헤더를 포함한 905행을 모두 세어").
- **이관 패키지에서는 "원래 폴더에 있던 경로"를 그대로 인용하면 확인 불가능한 근거가 된다.**
  푸터와 `dashboard.css` 주석이 `재료/design/DESIGN.md`·`stitch_.zip`을, 푸터가 `퀴즈제작현황_대시보드.html`
  (구본)을 가리키고 있었는데 셋 다 `README_시작하기.md` 6장이 "넣지 않았다"고 명시한 파일이었다.
  → 이관 후에는 **문서·주석에 남은 경로 인용도 패키지 안에 실재하는지 훑는다.** 없으면 지우거나
  "이 패키지에 없음"을 밝힌다.
- **라벨의 숫자와 실제 동작을 함께 바꾼다.** 아코디언의 "후보 A/B 검토 N건" 버튼이 `mine[0]` 하나만
  열도록 되어 있어, 미리보기가 2건이 된 02 대단원에서 "2건"인데 1건만 열렸다. 미리보기가 대단원당
  1건이던 동안에는 드러나지 않았다. → **개수를 세어 표시하는 자리는 그 개수만큼 동작도 나오는지**
  확인한다.

## 큐에 적힌 원고 파일명이 실제 파일과 다를 수 있다 (2026-09-10)

**무엇이 왜 발생했나.** `기록/진행상황.md` 4장 큐는 `P_1)_A자연과학소개_DF.docx.md`를 가리키고 있었지만
실제 파일은 `P_1)_A자연과학소개_최종_수정.md`였다. 원고가 개정되면서 팀이 파일명을 바꾼 것으로 보이는데
큐는 처음 만들 때의 이름 그대로였다. `퀴즈생성자`는 Read/Glob/Grep만 있어서 잘못된 절대경로를 받으면
파일을 못 열고, 최악의 경우 스스로 다른 원고를 고르는 사고(과거 2회)로 이어진다.

**재발 방지.** `퀴즈생성자`를 부르기 전에 **원고 폴더를 Glob으로 한 번 확인**하고, 큐의 경로와 다르면
실제 파일명을 호출 프롬프트에 적고 큐도 함께 고친다. 원고 처리 뒤 진행상황 갱신 시 파일명 변경 사실을
비고에 남긴다.

## 원고에 없는 "설명용 표현"이 순서배열 문장에 스며든다 (2026-09-10)

**무엇이 왜 발생했나.** `B. 과학 연구의 목표` 7B 순서배열 네 문장에 `원소를 성질의 규칙성에 따라
배열하고`·`주기율표의 규칙성에서 끌어내`·`그 성질이 측정되었다`가 들어갔다. 원고 17행에는 `규칙`·`배열`
문자열이 **한 번도 없고**(grep 0건), `주기율표에 빈자리를 남겨 두고 ... 성질을 예측했습니다`,
`발견되면서 ... 실제 성질과 잘 맞는다는 사실이 확인되었습니다`뿐이다. 주기율표가 규칙적 배열이라는 것은
**널리 알려진 배경지식**이어서 사실로는 맞지만, 규칙 3번(원본에 없는 값을 지어내지 않는다)과 SPEC Step 3
위반이다. 순서배열은 문장을 스스로 써야 해서 인과를 매끄럽게 만들려는 손질이 들어가기 쉽고, 그 손질이
배경지식을 끌어오는 경로가 된다. 정답 번호가 바뀌지 않으므로 `퀴즈JSON검증.js`로도 잡히지 않는다.

**재발 방지.** 순서배열 네 문장은 **원고의 해당 문단에 있는 표현만으로 쓴다.** 쓰고 난 뒤 각 문장의
핵심 명사·동사를 원고에서 grep해 실재를 확인하고, 없으면 원고 표현으로 바꾼다(문장을 지어내는 대신
원고 표현으로 되돌리는 방향). 사실로 맞는 배경지식일수록 눈에 걸리지 않으니 더 위험하다.

## 4번과 7번의 축이 겹치는데 대체 소재가 없을 때 (2026-09-10)

**무엇이 왜 발생했나.** v5는 4번=발견·논증의 역사적 전개, 7번=현상의 인과 연쇄로 축을 나누는데,
`B. 과학 연구의 목표` 7B가 멘델레예프의 역사 소재여서 4B(핼리)와 같은 각도가 되었다. 이 원고는
방법론 단원이라 순서를 정할 근거가 있는 소재가 **천둥 인과(11행)·핼리(17행)·멘델레예프(17행)·
뉴턴→상대성(21행)·지식 갱신 절차(25행)** 다섯뿐이고, 인과 연쇄는 천둥 하나뿐이다. 7A가 천둥을 쓰면
7B에 남는 인과 소재가 없다(A·B는 서로 달라야 하므로 — 규칙 7번).

**재발 방지.** 이런 원고에서는 **축을 맞추려고 없는 인과를 만들지 않는다.** 규칙 3번이 축 가이드보다
우선한다. 대신 ① 그 문항 `raw`에 축이 겹친 사실과 이유를 남기고 ② 동시 선택 주의 쌍으로 사람에게
보고한다. 대체 후보를 검토할 때는 **다른 자리와 새로 겹치지 않는지**까지 본다 — 이번에 대체안이었던
원고 25행(지식 갱신 절차)은 6B와 정면으로 겹쳐 옮기면 더 나빠지는 경우였다.

## 근거가 약한 순서배열을 "약하다"고만 적고 통과로 표기하면 안 된다 (2026-09-10)

**무엇이 왜 발생했나.** `A. 단위` 7B에서 `퀴즈생성자`가 자기검수 15번을 **"순서 근거 = 필연적 인과"
(통과)** 로 적고, 따로 "네 벌 중 근거가 가장 약하다 — 기획자 판단 대상"이라는 단서를 달았다. 실제로는
보기 `가`(미터가 빛의 속도에 연결되어 있다)와 `다`(1미터 = 빛이 299,792,458분의 1초 동안 나아가는 거리)가
**원고 13행이 나란히 쓴 같은 사실의 두 표현**이어서 교환해도 성립했다 — 규칙 4번이 "원고의 문장 순서만으로는
만들지 않는다"로 금지한 바로 그 경우다. **통과 표기 + 완곡한 단서**는 사람이 훑을 때 통과로 읽히고,
`퀴즈JSON검증.js`는 순서 근거를 볼 수 없어(구조만 검사) 아무도 잡지 못한다. 원인은 자리 네 개(4A·4B·7A·7B)를
채워야 한다는 압력이었다 — 원고의 순서배열 근거가 셋뿐인데 네 번째를 만든 것이다.

**재발 방지.** ① 순서배열은 **인접 문장 교환 테스트를 실제로 해 보고** 한 쌍이라도 뒤바꿔 성립하면
15번을 `미통과`로 적는다. "약하다"·"판단 대상" 같은 완곡어로 통과 표기를 유지하지 않는다(SPEC 6장:
임의로 고쳐 통과 처리하지 않는다). ② **네 벌을 채우지 못하는 원고가 있다** — 방법론·개념 단원은 순서
근거가 둘·셋뿐인 경우가 흔하다. 이때는 만들 수 있는 것만 만들고 근거 없음을 보고한다(v5 107행).
③ 대체 소재를 검토할 때는 **다른 자리와 새로 겹치지 않는지**까지 본다(이번 대체 후보는 5A 정답을
노출했다). ④ 다만 **문항을 지우는 것은 도구가 허용하지 않는다** — `퀴즈JSON검증.js` 71행이 자리마다
A·B 두 안을 요구한다. 그래서 지금 할 수 있는 처리는 `raw`에 미통과와 "고르지 않기를 권한다"를 명시하는
것까지다. 근거 없는 벌을 아예 비우려면 도구·SPEC을 함께 고쳐야 하므로 사람 결정 사항이다.

## `퀴즈생성자` 응답은 앞부분부터 잘릴 수 있다 (2026-09-10)

**무엇이 왜 발생했나.** `A. 단위` 응답이 **3B 발문 중간부터** 도착해 1번·2번·3A 문항 본문이 통째로
없었다. 뒤쪽(자기검수·축 설계·검수 요청)은 온전했다. 잘림이 뒤가 아니라 **앞**에서 일어나 "응답이 짧다"는
신호가 없었고, 자기검수 리포트가 14문항 전체를 통과로 보고하고 있어 그것만 보면 정상으로 보였다.

**재발 방지.** 진행상황 2-2장 1번의 잘림 확인을 **문항 번호를 1번부터 세는 방식**으로 한다(총량이나
마지막 항목 존재로 판단하지 않는다). 빠졌으면 **빠진 자리만** 다시 요청하고, 재요청 프롬프트에
"이미 만든 문항을 그대로 옮겨 적어라 · 내용을 새로 바꾸지 말 것 — 자기검수가 그 문항 기준으로 이미
작성되어 있다"와 **근거 행 번호를 함께 적어라**를 넣어 앞서 받은 보고값과 대조한다.

## "개념적 전제 관계"를 "사건의 순서"로 착각해 순서배열 15번을 통과로 적는다 (2026-09-11)

**무엇이 왜 발생했나.** `B. 과학 연구의 측정` 4B에서 `퀴즈생성자`가 15번을 **통과 · "필연적 인과"**로
보고했는데, 실제로 제시한 근거는 *"라(자가 알려 주는 자리까지만 기록할 수 있다)가 있어야 가(25.4321이
덧붙인 것이라는 판단)가 성립한다"* 같은 **개념적 전제 관계**였다. 개념적 전제는 **서술 순서를 강제하지
않는다** — 원칙을 먼저 말하고 사례를 들 수도, 결론을 먼저 말하고 근거를 댈 수도 있다. 규칙 4번이
요구하는 것은 "뒤바꾸면 **과학적으로 성립하지 않아야**"인데 이 벌은 뒤바꿔도 틀리지 않았고, 정답을
실제로 고정하는 것은 **원고 13행의 서술 순서**뿐이었다(규칙 4번이 명시적으로 금지한 경우).
2026-09-10 `A. 단위` 7B와 **원인이 다르다** — 그때는 두 문장이 같은 사실의 두 표현이라 교환 가능했고,
이번에는 문장 넷이 서로 다른 내용인데도 **순서를 강제하는 힘이 없었다.** 이쪽이 더 잡기 어렵다.

**재발 방지.** ① 15번을 판정할 때 **"A가 B의 전제인가"를 묻지 말고 "A와 B를 뒤바꾼 글이 과학적으로
틀리는가"를 묻는다.** 전자는 거의 항상 예라고 답하게 되어 통과 쪽으로 기운다. ② 근거를 적을 때
**어느 유형인지 이름을 붙인다** — `연도`(원고에 표시된 시대 표시) / `사건의 시간 순서`(현상·행위가
실제로 일어난 차례) / `논리적 필연`(뒤 문장이 앞 문장 없이는 성립 불가). **`설명의 흐름`·`서술 순서`·
`개념적 전제`는 근거가 아니다.** ③ 통과로 적은 벌도 **인접 교환 결과를 문장으로 남긴다**(예:
"다↔라 교환 시 원칙 → 사례가 되어 성립 → 미통과"). 결과를 적게 하면 검사를 건너뛸 수 없다.

**참고로 이번에 통과한 두 벌이 왜 통과인지**가 기준을 보여준다 — 4A는 `연도`(원고 31행 `1676년`),
7B는 `사건의 시간 순서`(요인 작용 → 값이 흩어짐 → 반복 측정 → 평균 추정)다. 둘 다 **글의 순서가
아니라 세계의 순서**에 근거한다.

## 순서배열 네 문장 중 둘이 "같은 말"이면 순서 근거보다 먼저 그것이 결함이다 (2026-09-11)

**무엇이 왜 발생했나.** 같은 건 7A에서 보기 `가`("여러 번 재어도 값이 비슷한 방향으로 어긋난다",
원고 21행)와 `라`("반복해도 한쪽으로 치우친 값은 그대로 남는다", 원고 23행)가 **사실상 같은 진술**이었다.
원고의 계통 오차 서술이 **원인 → 증상 → 대응 3단 구조**라 네 번째 단계가 없는데, 자리를 채워야 하니
**한 단계를 말만 바꿔 두 번 쓴** 것이다. 순서 근거가 없는 것(15번)은 그 결과였고, 근본 원인은 소재가
3단인데 4문장을 요구한 데 있다. 검토자가 15번 위반으로 보고했지만 **중복 자체는 어느 검사 항목에도
걸리지 않는다** — `퀴즈JSON검증.js`는 구조만 보고, 자기검수 4번의 중복 검사는 문항 간(3·6번, 4·7번)이지
**한 문항 안의 보기끼리**가 아니다.

**재발 방지.** ① 순서배열을 만든 뒤 **네 문장이 원고의 몇 단계에서 왔는지 세어 본다.** 두 문장이 같은
단계에서 왔으면 그 벌은 성립하지 않는다 — 순서를 고치는 게 아니라 **소재를 바꿔야** 한다.
② 그래도 소재가 없으면 문항을 남기되(도구가 자리마다 A·B를 요구하므로) **중복은 반드시 해소한다.**
이번에는 보기4를 `측정을 반복해도 잘못 보정된 저울이 저절로 고쳐지지는 않는다`로 바꿔 **측정값이
아니라 도구의 상태**를 말하게 했다 — 같은 원고 문장(23행)에서 **다른 층위**를 고르면 중복이 풀린다.
③ 미통과 보고와 중복 해소는 **별개의 일**이다. "어차피 고르지 말라고 할 문항"이라고 중복을 방치하면,
기획자가 그래도 골랐을 때 결함이 둘인 문항이 나간다.

## 산출물이 조용히 사라지는 일이 같은 파일에서 두 번 일어났다 (2026-09-11)

**무엇이 왜 발생했나.** `02_별_B_핵물리`의 미리보기 HTML이 2026-09-10 08:35~08:47 사이에 원인 불명으로
사라져 JSON에서 복구했는데, 2026-09-11에 보니 **JSON과 HTML이 둘 다 없다.** 대단원 CSV에도 미반영이라
**문항 14개를 복구할 원본이 어디에도 없다**(재생성해야 하며 `퀴즈생성자` 20여 분이 다시 든다).
발견 경위도 우연이었다 — 다른 원고를 처리하려고 `퀴즈데이터/*.json`을 Glob했다가 눈에 띄었다.
`진행상황.md`는 그동안 계속 "생성 완료 · 기획자 선택 대기"로 적고 있었다.

**재발 방지.** ① **세션을 시작할 때 `진행상황.md`가 "생성 완료"라고 적은 건수와 실제 파일 수를
대조한다.** Glob 두 번이면 되고 비용이 없다. 기록과 파일이 어긋나는 순간이 유실 시점이다.
② 같은 파일이 두 번 사라졌다면 우발적 삭제로 보지 않는다 — **동기화 도구·백업 프로그램·에디터의
임시 폴더 정리**처럼 폴더를 건드리는 외부 요인을 의심한다. ③ `build-dashboard.js`는 CSV와 퀴즈데이터를
읽어 "검토 대기" 건수를 세므로, **대시보드를 돌리면 사라진 건이 수치로 드러난다** — 근거로 보고하기
전에 한 번 돌리라는 기존 지침(진행상황 0장 3번)이 이 경우에도 방어선이 된다.
