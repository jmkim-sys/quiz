# 보관: 2026-08-28 구(舊)규칙으로 만든 퀴즈

2026-08-28 공통 지침 4건(순서배열 근거 / 비유·은유 금지 / 직접 입력 형식 / A·B 키워드 차별화)이
확정되면서, 그 이전 규칙으로 만든 퀴즈 24건을 여기로 옮겼다.

| 폴더 | 개수 | 원래 위치 |
|---|---|---|
| `퀴즈데이터/` | 24 | `산출물/퀴즈데이터/` |
| `퀴즈미리보기/` | 24 | `산출물/퀴즈미리보기/` |

## 이 폴더의 취급

- **참고용이다.** 여기 있는 문항을 그대로 CSV에 반영하지 않는다 — 새 지침을 통과하지 않은 문항이다.
- 새로 만들 때 **소재가 겹치는지 확인하거나, 살릴 만한 소재를 찾는 용도**로만 연다.
- `재료/`도 `산출물/`도 아니므로 `검토자`·`대시보드 생성기`의 기본 검토·집계 대상에 들어가지 않는다.
- 대단원별 CSV에는 애초에 아무것도 반영되어 있지 않았다(2026-08-18에 유일한 반영분을 비웠음).
  따라서 이 이동으로 잃은 확정 데이터는 없다.
